{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://supertedai.github.io/EFC/schema/regime_node.schema.json",
  "title": "EFC Regime Node Schema",
  "description": "Generic regime node schema: the container that every link in the EFC atlas is an instance of. Each node carries the whole observation chain and its ontological frame — so that the H2O phases, the L0-L3 cosmology and the rainbow can be described with the same structure. The schema claims no physics: it is a structure; fields that are hypotheses are marked as hypotheses (see observer.awareness). Anchoring: regime/proxy/placement/episenter/compression are defined in 'Regime-Bound Measurement in Complex Systems: Proxy, Placement, and Validity' (DOI 10.6084/m9.figshare.31564123, 2026-03-07) and the meta reference docs/papers/meta/Proxy/ — episenter there means 'the interpretive frame used to read the result'. Morten's extensions (target/measurer/instrument/proxy_chain, buffer, phase, ontology, observer bandwidth, emergence loop, fractal, coupling) are structural containers sourced in conversation 2026-09-16.",
  "type": "object",
  "required": [
    "@context",
    "@id",
    "@type",
    "schema_version",
    "generated_at",
    "nodes",
    "relations"
  ],
  "properties": {
    "@context": {
      "type": "object",
      "properties": {
        "@vocab": {
          "const": "https://supertedai.github.io/EFC/ontology#"
        },
        "id": {
          "const": "@id",
          "description": "Node ids must expand to @id — the instance is a linked graph, not loose text."
        },
        "subject": {
          "const": {
            "@type": "@id"
          },
          "description": "Relation endpoints are IRI references to nodes."
        },
        "object": {
          "const": {
            "@type": "@id"
          },
          "description": "Relation endpoints are IRI references to nodes."
        }
      },
      "required": [
        "@vocab",
        "id",
        "subject",
        "object"
      ],
      "additionalProperties": false
    },
    "@id": {
      "type": "string",
      "format": "uri"
    },
    "@type": {
      "const": "RegimeNodeAtlas"
    },
    "schema_version": {
      "type": "string"
    },
    "generated_at": {
      "type": "string",
      "format": "date-time"
    },
    "nodes": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/RegimeNode"
      }
    },
    "relations": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/RegimeRelation"
      }
    }
  },
  "$defs": {
    "RegimeNode": {
      "type": "object",
      "required": [
        "id",
        "regime",
        "phase",
        "measure",
        "episenter",
        "buffer",
        "ontology",
        "observer",
        "emergence",
        "fractal",
        "coupling",
        "perspektiv",
        "stipulasjoner",
        "epistemikk",
        "maale_paradigme",
        "nivaa",
        "synlighet"
      ],
      "properties": {
        "id": {
          "type": "string"
        },
        "regime": {
          "type": "object",
          "required": [
            "name",
            "validity"
          ],
          "properties": {
            "name": {
              "type": "string"
            },
            "validity": {
              "type": "string",
              "description": "The validity range — WHAT the node applies to. A regime without a declared validity is a claim without a boundary."
            },
            "law_form": {
              "type": "string",
              "description": "The law-form that holds in the regime (from the DOI paper: a single law-form fits)."
            }
          },
          "additionalProperties": false
        },
        "phase": {
          "type": "string",
          "description": "The state within the regime (e.g. solid/liquid/gas/supercritical)."
        },
        "measure": {
          "type": "object",
          "required": [
            "target",
            "measurer",
            "instrument",
            "proxy_chain",
            "placement",
            "compression"
          ],
          "properties": {
            "target": {
              "type": "string",
              "description": "The target: the unobserved/costly quantity one aims at (what the proxy stands for)."
            },
            "measurer": {
              "type": "string",
              "description": "The meter: that which performs the measurement (actor/mechanism)."
            },
            "instrument": {
              "type": "string",
              "description": "The measuring instrument: the physical device."
            },
            "proxy_chain": {
              "type": "array",
              "items": {
                "type": "string"
              },
              "minItems": 1,
              "description": "Proxychain: the ordered chain of proxies from the target to what is actually read out. Each link is a proxy with its own regime."
            },
            "placement": {
              "type": "string",
              "description": "The placement: where the observer sits (instrument, sample, coordinate system) and how the proxy is computed (the DOI paper)."
            },
            "compression": {
              "type": "string",
              "description": "The compression: the reduction of high-dimensional data to a low-dimensional summary (the DOI paper)."
            }
          },
          "additionalProperties": false
        },
        "episenter": {
          "type": "string",
          "description": "The epicenter: the interpretive frame the result is read in (the DOI paper: the interpretive frame used to read the result). Not «where the phenomenon sits» — it is which frame makes residual structure into signal. Canonical Norwegian term from the source README (docs/papers/meta/Proxy/)."
        },
        "buffer": {
          "type": "object",
          "required": [
            "role"
          ],
          "properties": {
            "role": {
              "type": "string",
              "description": "The buffer/holding role: the capacity or the inertia that damps change — broad but logical: thermal inertia (water holding the temperature during heating up to the boiling point, latent heat at the phase transition), chemical/electrical storage (battery), biological regulation (homeostasis in a body). The buffer is fractally present: every level, every scale and every axis has its own buffer (multiaxial)."
            },
            "note": {
              "type": "string"
            }
          },
          "additionalProperties": false
        },
        "ontology": {
          "type": "object",
          "required": [
            "assumes",
            "source"
          ],
          "properties": {
            "assumes": {
              "type": "array",
              "items": {
                "type": "string"
              },
              "description": "What the description assumes exists — the ontological starting points, written EXPLICITLY."
            },
            "source": {
              "type": "string",
              "description": "The source the assumptions rest on (DOI, paper, measured reference)."
            },
            "proveniens": {
              "type": "object",
              "required": [
                "kilder",
                "begrensning",
                "fullstendig"
              ],
              "properties": {
                "kilder": {
                  "type": "array",
                  "minItems": 1,
                  "items": {
                    "type": "object",
                    "required": [
                      "type",
                      "ref",
                      "hentet"
                    ],
                    "properties": {
                      "type": {
                        "enum": [
                          "doi",
                          "url",
                          "stroem",
                          "laerebok",
                          "artikkel",
                          "intern"
                        ]
                      },
                      "ref": {
                        "type": "string",
                        "minLength": 1
                      },
                      "hentet": {
                        "type": "string",
                        "minLength": 1
                      }
                    },
                    "additionalProperties": false
                  }
                },
                "begrensning": {
                  "type": "string",
                  "minLength": 1
                },
                "fullstendig": {
                  "type": "boolean"
                }
              },
              "additionalProperties": false
            }
          },
          "additionalProperties": false
        },
        "observer": {
          "type": "object",
          "required": [
            "bandwidth",
            "awareness",
            "er_del_av_systemet"
          ],
          "properties": {
            "bandwidth": {
              "type": "string",
              "description": "The observer's window: which part of the spectrum it sees. All of reality lies on a broad spectrum; an observer sees a few nm."
            },
            "awareness": {
              "type": "string",
              "enum": [
                "instrument_window",
                "hypothesis_open",
                "not_claimed"
              ],
              "description": "Consciousness status, honestly declared: instrument_window = physical instrument without a consciousness claim; hypothesis_open = open hypothesis (e.g. field resonance), not established; not_claimed = no claim."
            },
            "er_del_av_systemet": {
              "type": "boolean",
              "const": true,
              "description": "We are the measuring instrument itself, not an observer outside the system. Const true — a node that claims otherwise is an epistemic error for schema purposes."
            },
            "c_s_posisjon": {
              "type": "string",
              "minLength": 1,
              "description": "the C(S) point the observer reads from; bandwidth is the narrow window around the point."
            },
            "maalepavirkning": {
              "type": "string",
              "description": "How the measurement affects/bounds what is measured (back-action), if relevant."
            }
          },
          "additionalProperties": false
        },
        "emergence": {
          "type": "object",
          "required": [
            "loop",
            "properties"
          ],
          "properties": {
            "loop": {
              "type": "string",
              "description": "The emergence loop: the feedback that holds the emergent level up."
            },
            "properties": {
              "type": "array",
              "items": {
                "type": "string"
              },
              "description": "Emergent properties — they exist at this level, not in the parts alone."
            }
          },
          "additionalProperties": false
        },
        "fractal": {
          "type": "object",
          "required": [
            "pattern"
          ],
          "properties": {
            "pattern": {
              "type": "string",
              "description": "The fractal relation: how the same pattern repeats — every part is its own emergence with its own loops in the same landscape."
            },
            "note": {
              "type": "string"
            }
          },
          "additionalProperties": false
        },
        "coupling": {
          "type": "object",
          "required": [
            "local",
            "global"
          ],
          "properties": {
            "local": {
              "type": "string",
              "description": "Local role: what the node does where it is."
            },
            "global": {
              "type": "string",
              "description": "Global reach: what consequences the node has outside itself — systemic empathy, local-global coupling."
            },
            "empathy_note": {
              "type": "string"
            }
          },
          "additionalProperties": false
        },
        "perspektiv": {
          "type": "string",
          "enum": [
            "paradigme",
            "konsensus",
            "akademia",
            "agnostikk"
          ],
          "description": "Epistemic home: paradigme = our frame; konsensus = the prevailing worldview; akademia = the established knowledge base; agnostikk = the open"
        },
        "stipulasjoner": {
          "type": "object",
          "required": [
            "stipulert_av_oss"
          ],
          "properties": {
            "stipulert_av_oss": {
              "type": "boolean",
              "description": "True: the phase/regime boundaries are OUR definitions, not given lines of nature."
            },
            "terskler": {
              "type": "array",
              "items": {
                "type": "string"
              },
              "description": "The stipulated thresholds, with value and source."
            },
            "motor": {
              "type": "string",
              "description": "The engine that holds the threshold in code."
            },
            "buss_status": {
              "type": "string",
              "description": "Either the node has a buss_domene, OR it states here why it does not. An omission is not an answer — measured 2026-09-18 (t_fc25238b).",
              "minLength": 1
            },
            "motor_status": {
              "type": "string",
              "description": "Either the node has an engine, OR it says here why it does not. An instrument node needs no engine; it only has to say so.",
              "minLength": 1
            },
            "alene_status": {
              "type": "string",
              "minLength": 1,
              "description": "The node has no couplings to other nodes. Then it must say WHY here — «alone» is a valid answer, an empty field is not. Measured 2026-09-18 (t_7dd41524): 13 of 113 nodes stood this way."
            },
            "ikke_falsifiserbar_grunn": {
              "type": "string",
              "minLength": 1,
              "description": "Written ground when the node cannot be refuted by an observation: the instrument node IS the measurement, established physics is not ours to refute, or the self-description has no claim to refute."
            }
          },
          "description": "The stipulations are where we define the boundaries. Before epistemology v2 they lived hardcoded in the engines with no trace in the node.",
          "additionalProperties": false
        },
        "synlighet": {
          "type": "string",
          "enum": [
            "offentlig",
            "intern"
          ],
          "description": "Publication surface. public = generated to GitHub Pages; internal = lives only in the complete atlas. The filter is DECLARED per node, never a hidden rule in the generator — a node that disappears without anyone seeing why is the same error class that the rest of this schema exists to prevent."
        },
        "ville_falsifisere": {
          "type": "string",
          "minLength": 1,
          "description": "Which observation would kill this node's placement/claim. One of three answers that make the node take a position on whether it can be struck down: this, `falsifiserbarhet` (the criterion exists, the threshold does not yet) or `stipulasjoner.ikke_falsifiserbar_grunn`. That ONE of them EXISTS is held by the tests (`test_atlas_avgjorelse.py`, `test_atlas_motsigelse.py`) — but an empty string is not an answer here."
        },
        "open_questions": {
          "type": "array",
          "description": "Open questions at this node. This is the ONLY source of questions in the atlas: the generator never writes a question itself. Measured 2026-09-18: the generator invented a line for every node without evidence — identical for all seven — and made «7 open» one and the same word seven times. The evidence status is a measurement and belongs in `epistemikk`, not in the question list. An element is a text, or an object {q, r?, to?}: r = the answer once the question is answered, to = whom it waits for (a human action).",
          "items": {
            "oneOf": [
              {
                "type": "string",
                "minLength": 1
              },
              {
                "type": "object",
                "required": [
                  "q"
                ],
                "additionalProperties": false,
                "properties": {
                  "q": {
                    "type": "string",
                    "minLength": 1
                  },
                  "r": {
                    "type": "string",
                    "minLength": 1
                  },
                  "to": {
                    "type": "string",
                    "minLength": 1
                  }
                }
              }
            ]
          }
        },
        "revisjon": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Log of changed thresholds/assumptions (date + what)."
        },
        "epistemikk": {
          "type": "object",
          "required": [
            "sannhetsstatus",
            "evidensstatus",
            "konsensusstatus",
            "sosial_mekanisme",
            "konsensus_er_ikke_sannhet"
          ],
          "additionalProperties": false,
          "properties": {
            "sannhetsstatus": {
              "type": "string",
              "enum": [
                "uavklart",
                "hypotese",
                "modellrelativ",
                "stottet",
                "motbevist"
              ],
              "description": "How true the claim is — independently of who believes it."
            },
            "evidensstatus": {
              "type": "string",
              "enum": [
                "ingen",
                "proxy",
                "direkte",
                "uavhengig",
                "replikert"
              ],
              "description": "What kind of evidence the claim carries."
            },
            "konsensusstatus": {
              "type": "string",
              "enum": [
                "ingen",
                "minoritet",
                "omstridt",
                "institusjonell",
                "bred"
              ],
              "description": "Social uptake — a MECHANISM description, not truth status."
            },
            "sosial_mekanisme": {
              "type": "string",
              "description": "Which social forces carry the consensus: narrative strength, incentives, funding, prestige, ego, career risk."
            },
            "konsensus_er_ikke_sannhet": {
              "type": "boolean",
              "const": true,
              "description": "Morten's principle (e): consensus does not define the truth — humans are social animals that follow the strongest narrative."
            }
          },
          "description": "Epistemic accounting per node: truth, evidence and consensus status are ORTHOGONAL axes. Consensus is a social phenomenon; it does not carry truth in itself."
        },
        "analogi": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "avbildning": {
              "type": "string",
              "description": "What corresponds to what in the analogy."
            },
            "bryter_der": {
              "type": "string",
              "description": "The disanalogy: where the analogy breaks — mandatory when the node declares ANALOGOUS_TO."
            }
          },
          "description": "Analogy discipline: an analogy without a disanalogy hardens into ontology.",
          "required": [
            "avbildning",
            "bryter_der"
          ]
        },
        "maale_paradigme": {
          "type": "object",
          "required": [
            "koordinater",
            "enheter",
            "status"
          ],
          "additionalProperties": false,
          "properties": {
            "koordinater": {
              "type": "array",
              "items": {
                "type": "string",
                "enum": [
                  "tid",
                  "rom",
                  "masse",
                  "temperatur",
                  "energi",
                  "elektrisk_potensial",
                  "magnetfelt",
                  "fraksjon",
                  "trykk",
                  "ingen",
                  "hastighet"
                ]
              },
              "description": "The coordinate choices the node rests on — the assumed axes, not given."
            },
            "enheter": {
              "type": "string",
              "description": "The units the node measures in (SI or others)."
            },
            "status": {
              "type": "string",
              "enum": [
                "valgt_ramme",
                "avledet",
                "direkte_observerbar",
                "proxy"
              ],
              "description": "Is the coordinate a chosen frame, something derived, directly observable or a proxy?"
            },
            "alternativer": {
              "type": "array",
              "items": {
                "type": "string"
              },
              "description": "Alternative descriptions that were left out — awareness that other frames could have been chosen."
            },
            "s_regime": {
              "type": "string",
              "enum": [
                "Low-S",
                "S->0",
                "S~0",
                "S>0",
                "S->1",
                "high_s",
                "S~0.5"
              ],
              "description": "The EBE-S regime in which the node is valid; an explicit, rotatable S coordinate."
            },
            "sektor": {
              "type": "string",
              "enum": [
                "S",
                "D",
                "C"
              ],
              "description": "EFC sector: structural, dynamic or capacity."
            },
            "ebe_function": {
              "type": "string",
              "description": "The EBE function that binds claim validity to S, L and the proxy chain."
            },
            "klarhetsfunksjon": {
              "type": "string",
              "description": "The C(S) declaration for the node's clarity along the S axis."
            }
          },
          "description": "The coordinate paradigms (requirement d): time, space, mass and velocity are chosen coordinates — the node declares which ones it assumes."
        },
        "rcmp": {
          "type": "object",
          "required": [
            "instrument",
            "observabel",
            "teori",
            "overlap"
          ],
          "additionalProperties": false,
          "properties": {
            "instrument": {
              "type": "string"
            },
            "observabel": {
              "type": "string"
            },
            "teori": {
              "type": "string"
            },
            "overlap": {
              "const": true
            },
            "deklarasjon": {
              "type": "string"
            }
          },
          "description": "RCMP: instrument, observable and theory have overlapping domains of validity."
        },
        "nivaa": {
          "type": "object",
          "required": [
            "indeks",
            "forelder",
            "tidsskala",
            "lengdeskala"
          ],
          "additionalProperties": false,
          "properties": {
            "indeks": {
              "type": "integer",
              "description": "The plateau index: 0 = the substrate, increasing = more emerged level."
            },
            "forelder": {
              "type": [
                "string",
                "null"
              ],
              "description": "The node id of the plateau below — null for the root. The graph must be closed."
            },
            "tidsskala": {
              "type": "string",
              "description": "The plateau's characteristic time scale."
            },
            "lengdeskala": {
              "type": "string",
              "description": "The plateau's characteristic length scale."
            }
          },
          "description": "The plateau structure (requirement b): the recursion as a graph — each node declares its level, its parent and its scales. The fractal field is the prose; the level is the structure."
        },
        "buss_domene": {
          "type": "string",
          "description": "The bus domain this node describes, e.g. 'kosmos.kosmologi'. DECLARED on the node — not derived from a generator — so that the coupling can be verified in both directions. Absence means the node does not describe a bus domain."
        },
        "prediction": {
          "type": "object",
          "description": "The prediction contract MIRRORED from the bus (NATS VERDEN_PROGNOSE), not invented here. The contract already exists with a sealed DOI, SHA256, freeze, criterion, tolerance rule and arbiter status; the atlas had zero nodes carrying it while the weather domain settled 77 112 times. `correlation` is the stable key that carries both prediction and settlement.",
          "required": [
            "source",
            "issued_at",
            "valid_for",
            "expected",
            "correlation"
          ],
          "additionalProperties": false,
          "properties": {
            "source": {
              "type": "string"
            },
            "observable": {
              "type": "string"
            },
            "issued_at": {
              "type": "string"
            },
            "valid_for": {
              "type": "string"
            },
            "lead_time_minutes": {
              "type": "string",
              "description": "Lead time (`ledetid_min`). Measured as a STRING on the bus; no measured occurrence of numbers."
            },
            "expected": {
              "type": "string",
              "description": "expected as a JSON-encoded string — the same form the bus sends. The keys are observable-specific and cannot be closed in the schema without inventing a set we have not measured; the form is therefore mirrored faithfully."
            },
            "units": {
              "type": "string",
              "description": "units as a JSON-encoded string — the same form the bus sends. The keys are observable-specific and cannot be closed in the schema without inventing a set we have not measured; the form is therefore mirrored faithfully."
            },
            "correlation": {
              "type": "string"
            },
            "sealed_doi": {
              "type": "string"
            },
            "sealing_sha256": {
              "type": "string"
            },
            "freeze": {
              "type": "string",
              "description": "The freezing parameters (`frys`) as a JSON-encoded string — the same form the bus sends. The content has a known structure (primary_freeze/secondary_freeze with alpha, sampler, sha256_truncated, timestamp_utc), but the FORM is strict: the schema mirrors the form, and the test validates the structure by parsing it."
            },
            "arbiter": {
              "type": "string"
            },
            "arbiter_waiting_for": {
              "type": "string"
            },
            "criterion": {
              "type": "string"
            },
            "criteria": {
              "type": "string",
              "description": "criteria as a JSON-encoded string — the same form the bus sends. The keys are observable-specific and cannot be closed in the schema without inventing a set we have not measured; the form is therefore mirrored faithfully."
            },
            "tolerance_rule": {
              "type": "string"
            },
            "basis": {
              "type": "string"
            },
            "ebe_l_layer": {
              "type": "string"
            },
            "ebe_s_regime": {
              "type": "string"
            },
            "location": {
              "type": "string",
              "description": "Location from the message (`sted`) — the operative variant carries location, the sealed one does not."
            },
            "latitude": {
              "type": "string",
              "description": "Latitude (`lat`). Measured as a STRING on the bus (\"70.3554\") — not a number."
            },
            "longitude": {
              "type": "string",
              "description": "Longitude (`lon`). Measured as STRING."
            },
            "horizon_ordered_hours": {
              "type": "string",
              "description": "Ordered forecast horizon (`horisont_bestilt_timer`). Measured as STRING on the bus; no measured occurrence of any number."
            },
            "observed_at": {
              "type": "string",
              "description": "When the source issued/observed (`observert`)."
            }
          }
        },
        "settlement": {
          "type": "object",
          "description": "The settlement of a prediction. Carries the SAME `correlation` as prediction — the coupling is a shared key, not a new relation predicate (the predicate enum is closed). The weather domain writes forventet/utfall/avvik/utfall_kilde/utfall_tid/oppgjoer_versjon; this contract mirrors those fields.",
          "required": [
            "correlation",
            "outcome",
            "outcome_source"
          ],
          "additionalProperties": false,
          "properties": {
            "correlation": {
              "type": "string"
            },
            "outcome": {
              "type": "string",
              "description": "outcome as a JSON-encoded string — the same form the bus sends. The keys are observable-specific and cannot be closed in the schema without inventing a set we have not measured; the form is therefore mirrored faithfully."
            },
            "deviation": {
              "type": "string",
              "description": "deviation as a JSON-encoded string — the same form the bus sends. The keys are observable-specific and cannot be closed in the schema without inventing a set we have not measured; the form is therefore mirrored faithfully."
            },
            "outcome_source": {
              "type": "string"
            },
            "outcome_time": {
              "type": "string"
            },
            "settlement_version": {
              "type": "string"
            },
            "location": {
              "type": "string",
              "description": "Location from the settlement message (`sted`)."
            },
            "expected": {
              "type": "string",
              "description": "The expectation as it stood in the prediction (`forventet`). The settlement carries BOTH sides of the loop — that is why it can be read alone."
            },
            "lead_time_minutes": {
              "type": "string",
              "description": "Lead time (`ledetid_min`). Measured as a STRING on the bus; no measured occurrence of numbers."
            },
            "source": {
              "type": "string",
              "description": "The source that produced the prediction that was settled (`kilde`)."
            },
            "issued_at": {
              "type": "string",
              "description": "When the forecast was issued (`utstedt`)."
            },
            "valid_for": {
              "type": "string",
              "description": "The time the settlement applies to (`gyldig_for`)."
            },
            "observed_at": {
              "type": "string",
              "description": "When the measurement was made (`observert`)."
            }
          }
        },
        "falsifiserbarhet": {
          "type": "object",
          "description": "Why a node can NOT be refuted. Distinguish from `ville_falsifisere`: a stub cannot be refuted because it computes — it is a state description, not a falsifier, and it shall not count as satisfied falsifiability.",
          "required": [
            "status",
            "grunn"
          ],
          "additionalProperties": false,
          "properties": {
            "status": {
              "type": "string",
              "enum": [
                "stub",
                "terskel_ikke_fastsatt"
              ]
            },
            "grunn": {
              "type": "string"
            }
          }
        },
        "lagdeling": {
          "type": "object",
          "additionalProperties": false,
          "description": "A node can carry SEVERAL layers with different epistemic status. The biology is the core: the physiology is textbook (academia/supported), while the analogy to EFC is our own (paradigm/hypothesis). A single `perspektiv` field cannot carry both — hence this.",
          "properties": {
            "fysiologi": {
              "type": "object",
              "required": [
                "status",
                "kilde",
                "sannhet",
                "konsensus"
              ],
              "additionalProperties": false,
              "properties": {
                "status": {
                  "enum": [
                    "akademia",
                    "konsensus",
                    "paradigme",
                    "agnostikk"
                  ]
                },
                "kilde": {
                  "type": "string"
                },
                "sannhet": {
                  "enum": [
                    "stottet",
                    "hypotese",
                    "modellrelativ"
                  ]
                },
                "konsensus": {
                  "enum": [
                    "institusjonell",
                    "minoritet",
                    "ingen"
                  ]
                }
              }
            },
            "analogi": {
              "type": "object",
              "required": [
                "status",
                "kilde",
                "sannhet",
                "konsensus"
              ],
              "additionalProperties": false,
              "properties": {
                "status": {
                  "enum": [
                    "akademia",
                    "konsensus",
                    "paradigme",
                    "agnostikk"
                  ]
                },
                "kilde": {
                  "type": "string"
                },
                "sannhet": {
                  "enum": [
                    "stottet",
                    "hypotese",
                    "modellrelativ"
                  ]
                },
                "konsensus": {
                  "enum": [
                    "institusjonell",
                    "minoritet",
                    "ingen"
                  ]
                }
              }
            },
            "kilde": {
              "type": "object",
              "required": [
                "status",
                "kilde",
                "sannhet",
                "konsensus"
              ],
              "additionalProperties": false,
              "properties": {
                "status": {
                  "enum": [
                    "akademia",
                    "konsensus",
                    "paradigme",
                    "agnostikk"
                  ]
                },
                "kilde": {
                  "type": "string"
                },
                "sannhet": {
                  "enum": [
                    "stottet",
                    "hypotese",
                    "modellrelativ"
                  ]
                },
                "konsensus": {
                  "enum": [
                    "institusjonell",
                    "minoritet",
                    "ingen"
                  ]
                }
              }
            }
          }
        },
        "usikkerhet": {
          "type": "object",
          "description": "THE UNCERTAINTY LAYER (ADR-086 §3.1): which error bound THE SOURCE states for a value the node carries — or an explicit declaration that the source does not state it. Optional, closed and additive: no node must be changed for the atlas to validate, and a number without a stated uncertainty shall still be readable as exactly that. The diagnosis is measured: the headline numbers stand WITH an error bound in their own sources (`k = 0.415 ± 0.029` in docs/papers/efc/EFC_Phase_3__SPARC_Validation/index.json:5) while the atlas had 0 structured uncertainty fields. The field shall not procure new uncertainty — it shall stop losing it. `null` is a valid answer; 0 and a guess are not. The rule that cannot stand here (a post SHALL have a source that answers) is held by tests/test_usikkerhetslag.py until the C10 gate can be changed by human word (t_2e60afa6). Owner: Morten and Joakim (word 2026-09-18).",
          "required": [
            "poster"
          ],
          "additionalProperties": false,
          "properties": {
            "poster": {
              "type": "array",
              "minItems": 1,
              "items": {
                "$ref": "#/$defs/Usikkerhetspost"
              },
              "description": "One post per value the node carries with uncertainty. An empty list is not a post: a field that looks filled without being it is exactly the error this layer shall prevent."
            }
          }
        }
      },
      "additionalProperties": false
    },
    "Proveniens": {
      "type": "object",
      "required": [
        "kilder",
        "begrensning",
        "fullstendig"
      ],
      "properties": {
        "kilder": {
          "type": "array",
          "minItems": 1,
          "items": {
            "type": "object",
            "required": [
              "type",
              "ref",
              "hentet"
            ],
            "properties": {
              "type": {
                "enum": [
                  "doi",
                  "url",
                  "stroem",
                  "laerebok",
                  "artikkel",
                  "intern"
                ]
              },
              "ref": {
                "type": "string",
                "minLength": 1
              },
              "hentet": {
                "type": "string",
                "minLength": 1
              }
            },
            "additionalProperties": false
          }
        },
        "begrensning": {
          "type": "string",
          "minLength": 1
        },
        "fullstendig": {
          "type": "boolean"
        }
      },
      "additionalProperties": false
    },
    "RegimeRelation": {
      "type": "object",
      "required": [
        "subject",
        "predicate",
        "object"
      ],
      "properties": {
        "subject": {
          "type": "string"
        },
        "predicate": {
          "type": "string",
          "enum": [
            "TRANSITIONS_TO",
            "COUPLED_TO",
            "INSTANCE_OF",
            "EMERGES_FROM",
            "OBSERVED_THROUGH",
            "OBSERVED_IN",
            "ADDRESSES",
            "CARRIES",
            "ANALOGOUS_TO"
          ]
        },
        "object": {
          "type": "string"
        },
        "note": {
          "type": "string"
        }
      },
      "additionalProperties": false
    },
    "Lag": {
      "type": "object",
      "required": [
        "status",
        "kilde",
        "sannhet",
        "konsensus"
      ],
      "additionalProperties": false,
      "properties": {
        "status": {
          "enum": [
            "akademia",
            "konsensus",
            "paradigme",
            "agnostikk"
          ]
        },
        "kilde": {
          "type": "string"
        },
        "sannhet": {
          "enum": [
            "stottet",
            "hypotese",
            "modellrelativ"
          ]
        },
        "konsensus": {
          "enum": [
            "institusjonell",
            "minoritet",
            "ingen"
          ]
        }
      }
    },
    "Usikkerhetspost": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "storrelse",
        "verdi",
        "feilgrense",
        "kilde"
      ],
      "properties": {
        "storrelse": {
          "type": "string",
          "minLength": 1,
          "description": "The quantity as THE SOURCE writes it (e.g. «k», «β», «σ_int») — the source's name, not ours."
        },
        "verdi": {
          "type": [
            "number",
            "null"
          ],
          "description": "The value as the source states it. `null` is an answer when the source only states the bound."
        },
        "feilgrense": {
          "type": [
            "number",
            "null"
          ],
          "description": "The error bound THE SOURCE states on the named line. `null` means the source does not state it — that is an ANSWER, not a shortcoming, and β = 0.16 is the test. An error bound of 0 is valid only when the source itself says 0."
        },
        "kilde": {
          "$ref": "#/$defs/Usikkerhetskilde"
        }
      }
    },
    "Usikkerhetskilde": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "fil",
        "linje",
        "ordrett"
      ],
      "properties": {
        "fil": {
          "type": "string",
          "minLength": 1,
          "description": "Path from the repo root to the file the bound was read from. The file must be tracked in the repo — an untracked «source» does not exist as a source."
        },
        "linje": {
          "type": "integer",
          "minimum": 1,
          "description": "The line number in the file, 1-indexed. Read back by tests/test_usikkerhetslag.py: a guess does not stand on the line it points to."
        },
        "ordrett": {
          "type": "string",
          "minLength": 1,
          "description": "A VERBATIM excerpt of the line — enough to see that the source says what the post says. A quotation that does not stand on the line fells the test."
        }
      }
    }
  },
  "additionalProperties": false
}
