"""
EFC Physics Engines.

Each engine computes a physical observable from EFC parameters.
Implemented: rotation.py (galaxy rotation) and water.py (H2O phase
boundaries — the first non-cosmological regime engine). The rest are
stubs for Morten.
"""
