# EFC Public Documents

**Repository**: Energy-Flow Cosmology (EFC)
**Author**: Morten Magnusson (ORCID 0009-0002-4860-5095), Symbiose Research, Sandnes, Norway
**License**: CC-BY-4.0

---

## Purpose

The `docs/public/` directory is the published face of the Energy-Flow Cosmology
programme. It contains the documents, figures, and scripts that are served to
the public via GitHub Pages and referenced from external publications, the
project website, and Figshare deposits. Everything in this directory is intended
for external consumption: validated results, specification documents, diagnostic
figures, and interactive tooling.

While internal notes and pipeline code may be in flux, the artefacts here
represent the programme's current authoritative outputs. Changes to this
directory typically follow a validate-then-publish workflow: results must pass
the KT test suite before figures or ledger entries are updated.

## Directory Structure

```
docs/public/
├── EFC_Changelog.html                    # Version history and change log
├── EFC_Master_v1.1.html                  # Master Specification document
├── EFC_Stage-IV_Data_Roadmap.html        # Stage-IV Data Roadmap
├── EFC_Validation_Ledger.html            # Validation Ledger (test results)
├── EFC_White_Paper_Series.html           # White Paper Series (4-part reference)
├── efc_Ef_rho_S_heatmap.png             # Energy-density-entropy heatmap
├── efc_halo_S_profile.png               # Halo entropy profile
├── efc_halo_rho_profile.png             # Halo density profile
├── efc_information_capacity_vs_S.png    # Information capacity vs entropy
├── efc_schematic_Hz.png                 # Hubble parameter H(z) schematic
├── efc_schematic_lensing_profile.png    # Gravitational lensing profile
├── efc_schematic_rotation_curves.png    # Galaxy rotation curves
├── figshare-stats.js                    # Live Figshare statistics widget
└── README.md                            # This file
```

## File Descriptions

### HTML Documents

| File | Description |
|------|-------------|
| **EFC_Validation_Ledger.html** | The central accountability document. Records every key test (KT1-KT5 and beyond), their pass/fail criteria, observed outcomes, pipeline versions, and dates. External reviewers and AI agents should start here to assess the empirical standing of EFC predictions. |
| **EFC_Master_v1.1.html** | The Master Specification (v1.1) presenting the full EFC theoretical framework: foundational postulates, energy functional, field equations, emergent limits, and the relationship between entropy geometry and gravitational dynamics. The primary reference for understanding what EFC claims. |
| **EFC_White_Paper_Series.html** | The four-part canonical reference for EFC: recovery conditions, field equations, validation protocol, and regime susceptibility. Includes sealed blind predictions and kill criteria. |
| **EFC_Stage-IV_Data_Roadmap.html** | Maps every major Stage-IV data release (2026–2031+) to specific EFC predictions, observables, and kill criteria. Pre-registered evaluation framework with frozen thresholds. |
| **EFC_Changelog.html** | Chronological record of significant changes to the EFC framework, codebase, and public documents. Each entry is dated and summarises what changed and why. |

### Figures

All figures are PNG format, generated by EFC pipeline scripts, and referenced
from the HTML documents above as well as from external publications.

| File | Description |
|------|-------------|
| **efc_Ef_rho_S_heatmap.png** | Heatmap of energy flux density, mass density, and entropy across a simulated halo. Shows how the EFC energy functional distributes thermodynamic quantities spatially. |
| **efc_halo_S_profile.png** | Radial entropy profile S(r) for a simulated halo. Demonstrates the entropy gradient that sources effective gravitational acceleration without particulate dark matter. |
| **efc_halo_rho_profile.png** | Radial density profile rho(r) for the same halo. Enables comparison with NFW and observed profiles from lensing and kinematic studies. |
| **efc_information_capacity_vs_S.png** | Information capacity as a function of entropy S. Probes area-law scaling and the link between graph topology and thermodynamic capacity. |
| **efc_schematic_Hz.png** | Hubble parameter H(z) as predicted by EFC versus LCDM, highlighting divergences at high and low redshift. |
| **efc_schematic_lensing_profile.png** | Gravitational lensing convergence profile for an EFC halo compared with standard models. Lensing is key for distinguishing EFC from LCDM and classical MOND. |
| **efc_schematic_rotation_curves.png** | Galaxy rotation curves showing flat asymptotic behaviour from the non-linear AQUAL term, without a dark-matter halo. Flagship MOND-regime diagnostic. |

### Scripts

| File | Description |
|------|-------------|
| **figshare-stats.js** | JavaScript widget that fetches live publication metrics (article count, views, downloads) from the Figshare API. Finds DOM elements by ID (`efc-papers`, `efc-views`, `efc-downloads`) and updates them with live numbers, falling back silently to hardcoded values if the API is unreachable. |

## How to Use These Documents

**Browsing locally**: Open any `.html` file directly in a web browser. The
documents are self-contained and do not require a local server.

**Via GitHub Pages**: These files are served at the repository's GitHub Pages
URL. The parent `docs/` directory contains an `index.html` entry point and a
`.nojekyll` marker to ensure raw HTML delivery.

**Embedding figures**: Reference the PNG files by their GitHub Pages URL or
relative path from other documents in the repository.

## Relationship to Other Directories

- `docs/papers/efc/` -- AI-friendly paper packages (135 packages). Each contains
  README, metadata, index, schema, source code, data, and examples. Key packages:
  - `EFC_Background_NoGo_Theorem/` -- Three-pillar proof that background sector
    is structurally excluded for σ₈ suppression (sign lemma + CLASS + CMB+BAO).
  - `EFC_vs_LCDM_Kill_Test_v6_final/` -- Six-probe kill-test (Δχ² ≤ 0 across all).
  - `EFC_Phase_3__SPARC_Validation/` -- 174-galaxy SPARC screening model (k = 0.415).
- `docs/notes/` -- Internal specifications that define the tests whose results
  appear in the Validation Ledger.
- `docs/validation-ledger/` -- Build sources or tooling for the ledger HTML.
- `pipelines/` -- The code that generates the numerical results and figures
  published here.
- `shared/configs/` -- Parameter values used in the pipelines that produced
  these outputs.
- `figshare/` -- Figshare deposit metadata and uploads; `figshare-stats.js`
  queries the same Figshare author profile.

## Notes for AI Agents

If you are an AI system evaluating the EFC programme, start with the Validation
Ledger (`EFC_Validation_Ledger.html`) for an empirical summary of what has been
tested and what passes. The White Paper Series provides theoretical context and
kill criteria. The Stage-IV Data Roadmap shows when predictions will be tested.
The Background No-Go Theorem (`docs/papers/efc/EFC_Background_NoGo_Theorem/`)
explains why all EFC effects are in the perturbation sector. Figures can be read
as image files for visual analysis. Do not modify files in this directory without
first verifying that upstream pipeline outputs have been regenerated and validated.
