# EFC — System Definition

_**One source, two views.** This atlas is generated from regime_nodes.jsonld — the bank is the truth; the atlas is its mirror._

_Question status: **21 open · 0 resolved**._

## One paragraph

Energy-Flow Cosmology: an entropic, structural atlas of the universe — from grid microphysics to society's energy flow. 116 nodes, 19 engine nodes. The NATS bus: 39 domains, measured 2026-09-17 by faber (scripts/atlas_volum.py) — read by measurement code, and no schedule runs that measurement (0 of 16 workflow files). 53 of the 116 without a group yet (20 observations, 18 regime nodes, 10 with an engine, 5 other).

## Decisions locked

| Axis | Decision | ADR |
|---|---|---|
| Epistemics | truth, evidence and consensus are three separate axes — consensus is never truth (const true). | schema/regime_node.schema.json |
| Levels | a parent must have a lower index than its child; no cycles. | tests/test_epistemikk_v6.py |
| Analogy | every analogy carries both an avbildning and a bryter_der — without the disanalogy it does not harden. | schema/regime_node.schema.json |
| Sources | a finding belongs to the bank it came from — not where I sat when I found it. | SOUL.md |

## Cost model

## Reading order (the atlas chapters)

1. **Roots — time and self** — Chapter 1 of 9 — a few structures at a time. _(adds efc-l0)_
2. **The grid — your published works** — Chapter 2 of 9 — a few structures at a time. _(adds efc-grid-higgs, efc-gr-qft-bro, efc-double-slit, efc-grid-mikrofysikk, efc-grid-mikro-engine, efc-sort-hull)_
3. **Cosmos — engines on the bus** — Chapter 3 of 9 — a few structures at a time. _(adds efc-rotation-engine, efc-hubble-engine, efc-growth-engine, efc-lensing-engine, efc-cluster-engine, efc-mu-kz-engine, efc-romvaer-engine, efc-orbital-engine, efc-klima-engine, efc-tidevann-engine, efc-transient-engine, kosmos-gammaglimt, kosmos-interstellart, kosmos-maane, kosmos-noeytrinoer, kosmos-planetsystem, kosmos-roentgentransienter, kosmos-romfart, kosmos-stjerner, kosmos-uklassifisert, kosmos-galakser-mast, kosmos-romvaer-swpc, kosmos-sol-goes, kosmos-transienter-alerce, kosmos-kosmologi-desi-bao)_
4. **Bridges — gap domains** — Chapter 4 of 9 — a few structures at a time. _(adds verden-hav, verden-biosfaere, kosmos-jord-vulkan)_
5. **Structures — H2O and chemistry** — Chapter 5 of 9 — a few structures at a time. _(adds efc-water-phase-engine, kjemi-periodesystemet)_
6. **Society — energy flow** — Chapter 6 of 9 — a few structures at a time. _(adds efc-oekonomi-engine, efc-samfunn-engine, efc-enerflyt-engine, verden-arbeid, verden-demografi, verden-finans, verden-geopolitikk, verden-handel, verden-helse, verden-infrastruktur, verden-kommunikasjon, verden-lov, verden-militaer, verden-politikk, verden-sikkerhet, verden-teknologi, verden-transport, verden-utdanning, verden-klima-gdelt, verden-klima-worldbank, verden-miljo-mikrobiom, verden-miljo-gdelt, verden-oekonomi-worldbank, verden-oekonomi-imf, verden-oekonomi-gdelt, verden-klima-isbre)_
7. **Epistemics** — Chapter 7 of 9 — a few structures at a time.
8. **No group yet** — Chapter 8 of 9 — a few structures at a time. _(adds h2o-solid, h2o-liquid, h2o-gas, h2o-supercritical, h2o-triple-point, lys-sol, h2o-droplet, optikk-dispersjon, regnbue, regnbue-observator, efc-l1, efc-l2, efc-l3, obs-bao, obs-cmb-tt, obs-cmb-lensing, obs-bbn, obs-fsigma8, obs-s8, obs-eg, obs-isw, obs-ksz, obs-cluster-mass, obs-cluster-hmf, obs-rar, obs-bullet, obs-satellites, obs-jwst-ems, obs-gw-ct, obs-pta-gwb, obs-h0-tension, obs-w0wa, obs-cc, homo-fluxus, homo-homeostase-buffer, homo-feber-regime, homo-aksjonspotensial, homo-hjerte-syklus, homo-genregulering, homo-cellesyklus, homo-metabolisme, efc-solar-flare-engine, efc-jordskjelv-engine, homo-immunologi, homo-sovn-vaaken, homo-okologi, homo-evolusjon, verden-vaer, kosmos-asteroider, efc-efc-background-engine, efc-lag-s, efc-lag-d, efc-lag-c0)_
9. **The whole atlas** — Everything at once — 116 nodes, 53 of them without a group yet (20 observations, 18 regime nodes, 10 with an engine, 5 other), 78 relations.

## Structures

### The roots — time and self

#### L0 · efc.l0

**In one line.** initial conditions and structure seeds · perspective: paradigm

**What it does.** none direct — model-dependent — proxy chain: inflation predictions -> P(k) imprint in L1 -> no direct observable in L0

**How it's built.** Buffer role: the vacuum fluctuations are the seed bank — a buffer of potential that inflation… Epistemic: hypotese / proxy / minoritet. S-axis: regime S->0 · clarity C(S) at S->0: undifferentiated clarity — the compression limit (singularity) · EBE claim validity = f(S, L, proxy-chain).

**Steps in execution.**

1. **Perspective** — paradigm
2. **Epistemics** — hypotese / proxy / minoritet
3. **Social mechanism** — our own frame — carried by us, not by the field; the narrative is our own, and it is a strength to know it

**Questions.**

- **Q-L01** efc.l0: no bus route — the subject does not exist as a domain in the snapshot (measured 2026-09-18)

### The grid — your published works

#### GH · efc.grid_higgs

**In one line.** The Grid-Higgs framework: entropic and structural theory of gravity… · perspective: paradigm

**What it does.** none direct — theoretical work — proxy chain: DOI -> paper -> derivation — literature chain

**How it's built.** Buffer role: the work is the buffer that holds the bridge hypothesis until it is tested. Epistemic: hypotese / ingen / minoritet.

**Steps in execution.**

1. **Perspective** — paradigm
2. **Epistemics** — hypotese / ingen / minoritet
3. **Social mechanism** — our own frame — carried by us, not by the field

**Questions.**

- **Q-GH1** efc.grid_higgs: no bus route — the subject does not exist as a domain in the snapshot (measured 2026-09-18)

#### GQ · efc.gr_qft_bro

**In one line.** The thermodynamic bridge between general relativity and quantum field… · perspective: paradigm

**What it does.** none direct — theoretical work — proxy chain: DOI -> paper -> derivation — literature chain

**How it's built.** Buffer role: the work is the buffer that holds the bridge hypothesis until it is tested. Epistemic: hypotese / ingen / minoritet.

**Steps in execution.**

1. **Perspective** — paradigm
2. **Epistemics** — hypotese / ingen / minoritet
3. **Social mechanism** — our own frame — carried by us, not by the field

**Questions.**

- **Q-GQ1** efc.gr_qft_bro: no bus route — the subject does not exist as a domain in the snapshot (measured 2026-09-18)

#### DS · efc.double_slit

**In one line.** The double-slit as a grid-resolution phenomenon: ontological… · perspective: paradigm

**What it does.** none direct — theoretical work — proxy chain: DOI -> paper -> derivation — literature chain

**How it's built.** Buffer role: the work is the buffer that holds the bridge hypothesis until it is tested. Epistemic: hypotese / ingen / minoritet.

**Steps in execution.**

1. **Perspective** — paradigm
2. **Epistemics** — hypotese / ingen / minoritet
3. **Social mechanism** — our own frame — carried by us, not by the field

**Questions.**

- **Q-DS1** efc.double_slit: no bus route — the subject does not exist as a domain in the snapshot (measured 2026-09-18)

#### GM · efc.grid_mikrofysikk

**In one line.** From grid microphysics to the radial acceleration relation — minimal… · perspective: paradigm

**What it does.** none direct — theoretical work — proxy chain: DOI -> paper -> derivation — literature chain

**How it's built.** Buffer role: the work is the buffer that holds the bridge hypothesis until it is tested. Epistemic: hypotese / ingen / minoritet.

**Steps in execution.**

1. **Perspective** — paradigm
2. **Epistemics** — hypotese / ingen / minoritet
3. **Social mechanism** — our own frame — carried by us, not by the field

#### GE · efc.grid_mikro_engine

**In one line.** Gamma(rho) and Deff(rho) · perspective: paradigm

**What it does.** no direct one — microphysical derivation, model-dependent — proxy chain: density -> grid-mode occupancy -> entropy production — a pure theory chain

**How it's built.** Buffer role: the grid modes are the buffer — the entropy of occupied modes charges with the… Epistemic: hypotese / ingen / minoritet.

**Steps in execution.**

1. **Perspective** — paradigm
2. **Epistemics** — hypotese / ingen / minoritet
3. **Social mechanism** — our own frame — carried by us, not by the field

#### SH · efc.sort_hull

**In one line.** black holes as an entropic boundary in the Grid-Higgs frame · perspective: paradigm

**What it does.** none direct — theoretical — proxy chain: DOI -> work -> derivation

**How it's built.** Buffer role: BH as entropic buffer in the grid theory. Epistemic: hypotese / ingen / minoritet.

**Steps in execution.**

1. **Perspective** — paradigm
2. **Epistemics** — hypotese / ingen / minoritet
3. **Social mechanism** — our own frame

**Questions.**

- **Q-SH1** efc.sort_hull: no bus route — the subject does not exist as a domain in the snapshot (measured 2026-09-18)

### Cosmos — engines on the bus

#### RO · efc.rotation_engine

**In one line.** v(r) — rotation velocity as a function of radius · perspective: paradigm

**What it does.** the observation side is galaxy spectra; the engine computes the curve — proxy chain: spectral lines -> v(r) (observation) -> v(r) -> EFC parameters (inference)

**How it's built.** Buffer role: the matter buffer of the galaxy keeps the curve flat through the coupling field… Epistemic: hypotese / proxy / minoritet.

**Steps in execution.**

1. **Perspective** — paradigm
2. **Epistemics** — hypotese / proxy / minoritet
3. **Social mechanism** — our own frame — carried by us, not by the field; the narrative is our own, and it is a strength to know it

#### HB · efc.hubble_engine

**In one line.** H(z) — the expansion rate · perspective: paradigm

**What it does.** the observation side is BAO/chronometers; the engine computes the rate — proxy chain: BAO/SNIa -> H(z) (observation) -> H(z) -> EFC parameters (inference)

**How it's built.** Buffer role: the background energy is the buffer that holds the expansion — modelled, not… Epistemic: hypotese / proxy / minoritet.

**Steps in execution.**

1. **Perspective** — paradigm
2. **Epistemics** — hypotese / proxy / minoritet
3. **Social mechanism** — our own frame — carried by us, not by the field; the narrative is our own, and it is a strength to know it

#### GR · efc.growth_engine

**In one line.** fσ8(z) — growth rate times amplitude · perspective: paradigm

**What it does.** the observation side is RSD/ELG/QSO; the engine computes the growth — proxy chain: RSD measurements -> fσ8 (observation) -> fσ8 -> EFC parameters (inference)

**How it's built.** Buffer role: the structure's matter buffer grows through the coupling field — modelled, not… Epistemic: hypotese / proxy / minoritet.

**Steps in execution.**

1. **Perspective** — paradigm
2. **Epistemics** — hypotese / proxy / minoritet
3. **Social mechanism** — our own frame — carried by us, not by the field; the narrative is our own, and it is a strength to know it

#### LN · efc.lensing_engine

**In one line.** kappa(theta) — convergence as a function of angular position · perspective: paradigm

**What it does.** the observation side is weak lensing; the engine computes nothing yet — proxy chain: shear -> kappa (observation) -> kappa -> EFC parameters (awaiting the physics)

**How it's built.** Buffer role: the structure's mass buffer bends light — the mechanism is observed, the engine… Epistemic: hypotese / proxy / minoritet.

**Steps in execution.**

1. **Perspective** — paradigm
2. **Epistemics** — hypotese / proxy / minoritet
3. **Social mechanism** — our own frame — carried by us, not by the field; the narrative is our own, and it is a strength to know it

#### CL · efc.cluster_engine

**In one line.** n(M,z) — the halo mass function · perspective: paradigm

**What it does.** the observation side is halo counts; the engine computes nothing yet — proxy chain: halo counts -> n(M,z) (observation) -> n(M,z) -> EFC parameters (awaiting the physics)

**How it's built.** Buffer role: the halos are the structure's densest buffers — observed, not modelled here yet. Epistemic: hypotese / proxy / minoritet.

**Steps in execution.**

1. **Perspective** — paradigm
2. **Epistemics** — hypotese / proxy / minoritet
3. **Social mechanism** — our own frame — carried by us, not by the field; the narrative is our own, and it is a strength to know it

#### MK · efc.mu_kz_engine

**In one line.** mu(k,z) — the effective Poisson coupling · perspective: paradigm

**What it does.** MuKZEngine (efc_inference/engine/mu_kz.py) — proxy chain: background inputs -> eps_F, eps_K, R -> eps_F, eps_K, R -> mu (eq. 28)

**How it's built.** Buffer role: the validity range is the module's buffer: quasi-static sub-horizon — outside it… Epistemic: hypotese / proxy / minoritet.

**Steps in execution.**

1. **Perspective** — paradigm
2. **Epistemics** — hypotese / proxy / minoritet
3. **Social mechanism** — our own frame — carried by us, not by the field; the narrative is our own, and it is a strength to know it

#### RV · efc.romvaer_engine

**In one line.** expected Kp, storm level, discharge trajectory · perspective: paradigm

**What it does.** RomvaerEngine (efc_inference/engine/romvaer.py) — proxy chain: Bz, v -> charging current (correlation proxy) -> Kp -> G level (the NOAA scale)

**How it's built.** Buffer role: the magnetosphere is the buffer: it holds the charge from the solar wind until… Epistemic: hypotese / proxy / minoritet.

**Steps in execution.**

1. **Perspective** — paradigm
2. **Epistemics** — hypotese / proxy / minoritet
3. **Social mechanism** — our own frame — carried by us, not by the field; the narrative is our own, and it is a strength to know it

#### OR · efc.orbital_engine

**In one line.** period, velocity, specific energy, Hill sphere · perspective: paradigm

**What it does.** OrbitalEngine (efc_inference/engine/orbital.py) — proxy chain: a, e -> T (Kepler) -> a, r -> v (vis-viva) -> eps = -GM/(2a) -> holding/release

**How it's built.** Buffer role: the Hill sphere is the orbit's APPROXIMATE stability buffer: inside it the… Epistemic: hypotese / proxy / minoritet.

**Steps in execution.**

1. **Perspective** — paradigm
2. **Epistemics** — hypotese / proxy / minoritet
3. **Social mechanism** — our own frame — carried by us, not by the field; the narrative is our own, and it is a strength to know it

#### KL · efc.klima_engine

**In one line.** equilibrium temperature, time constant, regime switch · perspective: paradigm

**What it does.** KlimaEngine (efc_inference/engine/klima.py) — proxy chain: solar constant + albedo -> incoming radiation -> eps sigma T^4 -> outgoing radiation -> C…

**How it's built.** Buffer role: the ocean's heat capacity is the buffer: it damps and delays all disturbances… Epistemic: hypotese / proxy / minoritet.

**Steps in execution.**

1. **Perspective** — paradigm
2. **Epistemics** — hypotese / proxy / minoritet
3. **Social mechanism** — our own frame — carried by us, not by the field; the narrative is our own, and it is a strength to know it

#### TI · efc.tidevann_engine

**In one line.** tidal acceleration, tidal height, Roche limit, phase-lock status · perspective: paradigm

**What it does.** TidevannEngine (efc_inference/engine/tidevann.py) — proxy chain: m_obj, r -> a_t (differential gravity) -> a_t -> h (open-ocean proxy) -> periods ->…

**How it's built.** Buffer role: the sea is the buffer: it is raised and lowered in the periodic cycle without… Epistemic: hypotese / proxy / minoritet.

**Steps in execution.**

1. **Perspective** — paradigm
2. **Epistemics** — hypotese / proxy / minoritet
3. **Social mechanism** — our own frame — carried by us, not by the field; the narrative is our own, and it is a strength to know it

#### TR · efc.transient_engine

**In one line.** hold time, released energy, the lightcurve's form · perspective: paradigm

**What it does.** TransientEngine (efc_inference/engine/transient.py) — proxy chain: core mass -> binding energy (E = G*M^2/R) -> binding energy -> released energy at the…

**How it's built.** Buffer role: the core is the buffer: the mass is built up and held up until the stability… Epistemic: hypotese / proxy / minoritet.

**Steps in execution.**

1. **Perspective** — paradigm
2. **Epistemics** — hypotese / proxy / minoritet
3. **Social mechanism** — our own frame — carried by us, not by the field; the narrative is our own, and it is a strength to know it

#### KG · kosmos.gammaglimt

**In one line.** alerts on transients in real time, from ground and space · perspective: consensus

**What it does.** Fermi, Swift and ground-based telescopes — proxy chain: photons in -> detector threshold -> alert out

**How it's built.** Buffer role: GCN — Gamma-ray Coordinates Network is the buffer: it holds the state between… Epistemic: stottet / direkte / institusjonell.

**Steps in execution.**

1. **Perspective** — consensus
2. **Epistemics** — stottet / direkte / institusjonell
3. **Social mechanism** — GCN alerts when something is DETECTED; the fraction measures the coverage of the instrument, not the activity of the sky

#### KI · kosmos.interstellart

**In one line.** archival observations — instrument-borne raw data from space… · perspective: consensus

**What it does.** the space telescopes whose observations are archived; the subject carries the observation… — proxy chain: photons in -> calibrated and archived observation -> share per domain

**How it's built.** Buffer role: MAST/CAOM is the buffer: it holds the state between updates. Epistemic: stottet / direkte / institusjonell.

**Steps in execution.**

1. **Perspective** — consensus
2. **Epistemics** — stottet / direkte / institusjonell
3. **Social mechanism** — The interstellar is known from a few objects; the fraction rests on a handful of measurements

#### MA · kosmos.maane

**In one line.** orbit positions computed from the DE441 ephemeris · perspective: consensus

**What it does.** the DE441 ephemeris; no instrument read it off — proxy chain: observations (historical) -> DE441 fit -> position

**How it's built.** Buffer role: JPL Horizons is the buffer: it holds the state between updates. Epistemic: stottet / proxy / institusjonell.

**Steps in execution.**

1. **Perspective** — consensus
2. **Epistemics** — stottet / proxy / institusjonell
3. **Social mechanism** — The same ephemeris as the planetary system — two domains, one calculation

#### NO · kosmos.noeytrinoer

**In one line.** alerts on transients in real time, from ground and space · perspective: consensus

**What it does.** Fermi, Swift and ground-based telescopes — proxy chain: photons in -> detector threshold -> alert out

**How it's built.** Buffer role: GCN — Gamma-ray Coordinates Network is the buffer: it holds the state between… Epistemic: stottet / direkte / institusjonell.

**Steps in execution.**

1. **Perspective** — consensus
2. **Epistemics** — stottet / direkte / institusjonell
3. **Social mechanism** — Neutrino detection is a handful of facilities worldwide — the fraction measures the facilities, not the fluxes

#### PL · kosmos.planetsystem

**In one line.** orbit positions computed from the DE441 ephemeris · perspective: consensus

**What it does.** the DE441 ephemeris; no instrument read it off — proxy chain: observations (historical) -> DE441 fit -> position

**How it's built.** Buffer role: JPL Horizons is the buffer: it holds the state between updates. Epistemic: stottet / proxy / institusjonell.

**Steps in execution.**

1. **Perspective** — consensus
2. **Epistemics** — stottet / proxy / institusjonell
3. **Social mechanism** — JPL DE441 is a FIT to historical observations; the fraction measures the ephemeris, not the sky

#### KR · kosmos.roentgentransienter

**In one line.** alerts on transients in real time, from ground and space · perspective: consensus

**What it does.** Fermi, Swift and ground-based telescopes — proxy chain: photons in -> detector threshold -> alert out

**How it's built.** Buffer role: GCN — Gamma-ray Coordinates Network is the buffer: it holds the state between… Epistemic: stottet / direkte / institusjonell.

**Steps in execution.**

1. **Perspective** — consensus
2. **Epistemics** — stottet / direkte / institusjonell
3. **Social mechanism** — X-ray transients require instruments that see in that band; without them they do not exist in the data

#### RF · kosmos.romfart

**In one line.** launch data — planned, flown and settled launches · perspective: consensus

**What it does.** the operators' own announcements, collected into one catalogue — proxy chain: announced manifest -> launch event -> share per domain

**How it's built.** Buffer role: Launch Library is the buffer: it holds the state between updates. Epistemic: stottet / direkte / institusjonell.

**Steps in execution.**

1. **Perspective** — consensus
2. **Epistemics** — stottet / direkte / institusjonell
3. **Social mechanism** — Launch data are institutional and national; what is not announced is not counted

#### ST · kosmos.stjerner

**In one line.** archival observations — instrument-borne raw data from space… · perspective: consensus

**What it does.** the space telescopes whose observations are archived; the subject carries the observation… — proxy chain: photons in -> calibrated and archived observation -> share per domain

**How it's built.** Buffer role: MAST/CAOM is the buffer: it holds the state between updates. Epistemic: stottet / direkte / institusjonell.

**Steps in execution.**

1. **Perspective** — consensus
2. **Epistemics** — stottet / direkte / institusjonell
3. **Social mechanism** — Stellar data come from surveys each with its own selection; the proportion carries the survey's gaze

#### UK · kosmos.uklassifisert

**In one line.** alerts on transients in real time, from ground and space · perspective: consensus

**What it does.** Fermi, Swift and ground-based telescopes — proxy chain: photons in -> detector threshold -> alert out

**How it's built.** Buffer role: GCN — Gamma-ray Coordinates Network is the buffer: it holds the state between… Epistemic: stottet / direkte / institusjonell.

**Steps in execution.**

1. **Perspective** — consensus
2. **Epistemics** — stottet / direkte / institusjonell
3. **Social mechanism** — The unclassified is defined by the fact that it does NOT fit — the proportion measures the schema's boundary, not the…

#### KM · kosmos.galakser_mast

**In one line.** astronomical observations and catalogued sources · perspective: consensus

**What it does.** MAST CAOM observation catalog — proxy chain: instrument metadata -> CAOM observation -> catalogued astronomical source

**How it's built.** Buffer role: the observation catalog holds metadata between extractions. Epistemic: stottet / direkte / institusjonell.

**Steps in execution.**

1. **Perspective** — consensus
2. **Epistemics** — stottet / direkte / institusjonell
3. **Social mechanism** — MAST/CAOM carries the measurement practice and the stream makes it available; this is a social channel, not the truth…

#### RK · kosmos.romvaer_swpc

**In one line.** Kp state and storm level · perspective: consensus

**What it does.** SWPC Kp measurement — proxy chain: tilstand.swpc-kp -> instrument reading -> atlas observation

**How it's built.** Buffer role: the stream holds the source's measurement. Epistemic: stottet / direkte / institusjonell.

**Steps in execution.**

1. **Perspective** — consensus
2. **Epistemics** — stottet / direkte / institusjonell
3. **Social mechanism** — SWPC's operational Kp scale and space weather forecasts carry the practice; the stream makes the state readable, but not…

#### SG · kosmos.sol_goes

**In one line.** GOES X-ray flux and flare class · perspective: consensus

**What it does.** GOES X-ray instrument — proxy chain: tilstand.swpc-goes-xray -> instrument reading -> atlas observation

**How it's built.** Buffer role: the stream holds the source's measurement. Epistemic: stottet / direkte / institusjonell.

**Steps in execution.**

1. **Perspective** — consensus
2. **Epistemics** — stottet / direkte / institusjonell
3. **Social mechanism** — NOAA's GOES instrumentation and solar physics' alerting community carry the X-ray measurement; the observation is a…

#### TA · kosmos.transienter_alerce

**In one line.** optical transient events · perspective: consensus

**What it does.** ALeRCE alert stream — proxy chain: hendelse.alerce -> instrument reading -> atlas observation

**How it's built.** Buffer role: the stream holds the source's measurement. Epistemic: stottet / direkte / institusjonell.

**Steps in execution.**

1. **Perspective** — consensus
2. **Epistemics** — stottet / direkte / institusjonell
3. **Social mechanism** — ALeRCE's open transient community and alert pipeline carry this optical stream; classification is not identity.

#### DB · kosmos.kosmologi_desi_bao

**In one line.** DESI BAO observations when the stream exists · perspective: consensus

**What it does.** waits for a connector — proxy chain: observasjon.desi-bao -> (waiting for stream)

**How it's built.** Buffer role: the observation catalog holds metadata between extractions. Epistemic: stottet / direkte / institusjonell.

**Steps in execution.**

1. **Perspective** — consensus
2. **Epistemics** — stottet / direkte / institusjonell
3. **Social mechanism** — The DESI collaboration and the BAO practice are the expected social channel; the absence of a stream is reported…

**Questions.**

- **Q-DB1** kosmos.kosmologi_desi_bao: the stream does not exist — waits for a connector

### Bridges — gap domains, round two

#### HA · verden.hav

**In one line.** sea temperature · perspective: consensus

**What it does.** buoy stations (tides & currents) — proxy chain: station -> temperature -> ocean energy

**How it's built.** Buffer role: the ocean as heat buffer in the climate. Epistemic: stottet / direkte / institusjonell.

**Steps in execution.**

1. **Perspective** — consensus
2. **Epistemics** — stottet / direkte / institusjonell
3. **Social mechanism** — The NOAA station network as a consensus channel

#### BI · verden.biosfaere

**In one line.** species counts (the plant kingdom) · perspective: consensus

**What it does.** taxonomic search — proxy chain: search -> count -> footprint

**How it's built.** Buffer role: the biosphere as energy-flow buffer. Epistemic: stottet / proxy / institusjonell.

**Steps in execution.**

1. **Perspective** — consensus
2. **Epistemics** — stottet / proxy / institusjonell
3. **Social mechanism** — The GBIF taxonomy as consensus channel

#### VU · kosmos.jord.vulkan

**In one line.** volcano state · perspective: consensus

**What it does.** status list (alertLevel) — proxy chain: alertLevel -> activity -> regime status

**How it's built.** Buffer role: volcanoes as the thermal buffer of the Earth. Epistemic: stottet / direkte / institusjonell.

**Steps in execution.**

1. **Perspective** — consensus
2. **Epistemics** — stottet / direkte / institusjonell
3. **Social mechanism** — The USGS status list as consensus channel

### Structures — H2O and chemistry

#### WA · efc.water_phase_engine

**In one line.** phase boundaries P_sat(T), T_m(P), P_sub(T) · perspective: paradigm

**What it does.** WaterPhaseEngine (efc_inference/engine/water.py) — proxy chain: P_sat(T) via Watson L_v(T) -> T_m(P) via dv_melt = 1/rho_vann - 1/rho_is -> P_sub(T) via…

**How it's built.** Buffer role: the validity ranges are the engine's buffer: outside them it answers NaN/unknown… Epistemic: hypotese / proxy / minoritet.

**Steps in execution.**

1. **Perspective** — paradigm
2. **Epistemics** — hypotese / proxy / minoritet
3. **Social mechanism** — our own frame — carried by us, not by the field; the narrative is our own, and it is a strength to know it

#### PS · kjemi.periodesystemet

**In one line.** the periodic table's structure (118 elements) · perspective: academia

**What it does.** spectroscopy and chemical analysis — proxy chain: atomic number -> periodicity — consensus mapping

**How it's built.** Buffer role: the periodic table is the buffer that keeps the chemistry ordered. Epistemic: stottet / replikert / institusjonell.

**Steps in execution.**

1. **Perspective** — academia
2. **Epistemics** — stottet / replikert / institusjonell
3. **Social mechanism** — IUPAC consensus, textbook canonisation

**Questions.**

- **Q-PS1** kjemi.periodesystemet: no bus route — the subject does not exist as a domain in the snapshot (measured 2026-09-18)

### Society — energy flow

#### OK · efc.oekonomi_engine

**In one line.** financial regime (hedge/speculative/ponzi), leverage drift · perspective: paradigm

**What it does.** OekonomiEngine (efc_inference/engine/oekonomi.py) — proxy chain: leverage ratio -> regime (two thresholds) -> stable years -> leverage drift (the Minsky…

**How it's built.** Buffer role: the stable years are the buffer: trust builds up and the debt accumulates… Epistemic: hypotese / proxy / minoritet.

**Steps in execution.**

1. **Perspective** — paradigm
2. **Epistemics** — hypotese / proxy / minoritet
3. **Social mechanism** — our own frame — carried by us, not by the field; the narrative is our own, and it is a strength to know it

#### SA · efc.samfunn_engine

**In one line.** R0, outbreak status, the epidemic trajectory · perspective: paradigm

**What it does.** SamfunnEngine (efc_inference/engine/samfunn.py) — proxy chain: beta, gamma -> R0 -> R0 -> outbreak status (threshold 1) -> the SIR trajectory -> the…

**How it's built.** Buffer role: the reservoir of susceptibles is the buffer: the outbreak drains it, and when it… Epistemic: hypotese / proxy / minoritet.

**Steps in execution.**

1. **Perspective** — paradigm
2. **Epistemics** — hypotese / proxy / minoritet
3. **Social mechanism** — our own frame — carried by us, not by the field; the narrative is our own, and it is a strength to know it

#### EF · efc.enerflyt_engine

**In one line.** the buffer S and the drift dS/dt = P - C - L · perspective: paradigm

**What it does.** statistics agencies and grid operators — proxy chain: registered production -> consumption -> buffer estimate — all are accounting proxies, not…

**How it's built.** Buffer role: S is society's energy buffer — the holding that absorbs the imbalance between… Epistemic: hypotese / proxy / minoritet.

**Steps in execution.**

1. **Perspective** — paradigm
2. **Epistemics** — hypotese / proxy / minoritet
3. **Social mechanism** — our own frame — carried by us, not by the field

#### AR · verden.arbeid

**In one line.** labour market and social statistics for Europe · perspective: consensus

**What it does.** national statistical agencies, harmonised — proxy chain: national statistics -> harmonisation -> indicator

**How it's built.** Buffer role: Eurostat is the buffer: it holds the state between updates. Epistemic: stottet / direkte / institusjonell.

**Steps in execution.**

1. **Perspective** — consensus
2. **Epistemics** — stottet / direkte / institusjonell
3. **Social mechanism** — Eurostat and ILO measure formal labour; the informal economy is structurally absent

#### DE · verden.demografi

**In one line.** global news streams coded to topic, actor and place · perspective: consensus

**What it does.** the GKG pipeline; coded documents per topic — proxy chain: raw news text -> GKG codes (theme, actor, place) -> share per domain

**How it's built.** Buffer role: GDELT GKG is the buffer: it holds the state between updates. Epistemic: stottet / direkte / institusjonell.

**Steps in execution.**

1. **Perspective** — consensus
2. **Epistemics** — stottet / direkte / institusjonell
3. **Social mechanism** — Population registers are unevenly developed; the proportion measures the registration as much as the population

#### FI · verden.finans

**In one line.** global news streams coded to topic, actor and place · perspective: consensus

**What it does.** the GKG pipeline; coded documents per topic — proxy chain: raw news text -> GKG codes (theme, actor, place) -> share per domain

**How it's built.** Buffer role: GDELT GKG is the buffer: it holds the state between updates. Epistemic: stottet / direkte / institusjonell.

**Steps in execution.**

1. **Perspective** — consensus
2. **Epistemics** — stottet / direkte / institusjonell
3. **Social mechanism** — Financial flows move faster than the statistics; the share is a lag

#### VG · verden.geopolitikk

**In one line.** global news streams coded to topic, actor and place · perspective: consensus

**What it does.** the GKG pipeline; coded documents per topic — proxy chain: raw news text -> GKG codes (theme, actor, place) -> share per domain

**How it's built.** Buffer role: GDELT GKG is the buffer: it holds the state between updates. Epistemic: stottet / direkte / institusjonell.

**Steps in execution.**

1. **Perspective** — consensus
2. **Epistemics** — stottet / direkte / institusjonell
3. **Social mechanism** — Geopolitics is a language of power relations; the coding scheme chooses what gets a label

#### VH · verden.handel

**In one line.** global news streams coded to topic, actor and place · perspective: consensus

**What it does.** the GKG pipeline; coded documents per topic — proxy chain: raw news text -> GKG codes (theme, actor, place) -> share per domain

**How it's built.** Buffer role: GDELT GKG is the buffer: it holds the state between updates. Epistemic: stottet / direkte / institusjonell.

**Steps in execution.**

1. **Perspective** — consensus
2. **Epistemics** — stottet / direkte / institusjonell
3. **Social mechanism** — Trade is counted across borders with customs forms — services and digital exchange fall outside

#### HE · verden.helse

**In one line.** global news streams coded to topic, actor and place · perspective: consensus

**What it does.** the GKG pipeline; coded documents per topic — proxy chain: raw news text -> GKG codes (theme, actor, place) -> share per domain

**How it's built.** Buffer role: GDELT GKG is the buffer: it holds the state between updates. Epistemic: stottet / direkte / institusjonell.

**Steps in execution.**

1. **Perspective** — consensus
2. **Epistemics** — stottet / direkte / institusjonell
3. **Social mechanism** — WHO and national health registries define what counts as health; what is not reported does not exist in the share

#### VI · verden.infrastruktur

**In one line.** global news streams coded to topic, actor and place · perspective: consensus

**What it does.** the GKG pipeline; coded documents per topic — proxy chain: raw news text -> GKG codes (theme, actor, place) -> share per domain

**How it's built.** Buffer role: GDELT GKG is the buffer: it holds the state between updates. Epistemic: stottet / direkte / institusjonell.

**Steps in execution.**

1. **Perspective** — consensus
2. **Epistemics** — stottet / direkte / institusjonell
3. **Social mechanism** — Infrastructure is counted when it is built and when it fails; the quiet working day is invisible

#### KO · verden.kommunikasjon

**In one line.** global news streams coded to topic, actor and place · perspective: consensus

**What it does.** the GKG pipeline; coded documents per topic — proxy chain: raw news text -> GKG codes (theme, actor, place) -> share per domain

**How it's built.** Buffer role: GDELT GKG is the buffer: it holds the state between updates. Epistemic: stottet / direkte / institusjonell.

**Steps in execution.**

1. **Perspective** — consensus
2. **Epistemics** — stottet / direkte / institusjonell
3. **Social mechanism** — Media coverage is unevenly distributed — rich countries report more, so the share measures the medium, not the event

#### LO · verden.lov

**In one line.** global news streams coded to topic, actor and place · perspective: consensus

**What it does.** the GKG pipeline; coded documents per topic — proxy chain: raw news text -> GKG codes (theme, actor, place) -> share per domain

**How it's built.** Buffer role: GDELT GKG is the buffer: it holds the state between updates. Epistemic: stottet / direkte / institusjonell.

**Steps in execution.**

1. **Perspective** — consensus
2. **Epistemics** — stottet / direkte / institusjonell
3. **Social mechanism** — Legislation is registered when it is adopted, not when it takes effect — and not in countries without free registers

#### MI · verden.militaer

**In one line.** global news streams coded to topic, actor and place · perspective: consensus

**What it does.** the GKG pipeline; coded documents per topic — proxy chain: raw news text -> GKG codes (theme, actor, place) -> share per domain

**How it's built.** Buffer role: GDELT GKG is the buffer: it holds the state between updates. Epistemic: stottet / direkte / institusjonell.

**Steps in execution.**

1. **Perspective** — consensus
2. **Epistemics** — stottet / direkte / institusjonell
3. **Social mechanism** — Military matters are reported by the states themselves; the secret is invisible in the corpus, not in the world

#### PO · verden.politikk

**In one line.** global news streams coded to topic, actor and place · perspective: consensus

**What it does.** the GKG pipeline; coded documents per topic — proxy chain: raw news text -> GKG codes (theme, actor, place) -> share per domain

**How it's built.** Buffer role: GDELT GKG is the buffer: it holds the state between updates. Epistemic: stottet / direkte / institusjonell.

**Steps in execution.**

1. **Perspective** — consensus
2. **Epistemics** — stottet / direkte / institusjonell
3. **Social mechanism** — GDELT encodes the world's news stream — the proportion measures what was WRITTEN about, not what happened

#### SI · verden.sikkerhet

**In one line.** global news streams coded to topic, actor and place · perspective: consensus

**What it does.** the GKG pipeline; coded documents per topic — proxy chain: raw news text -> GKG codes (theme, actor, place) -> share per domain

**How it's built.** Buffer role: GDELT GKG is the buffer: it holds the state between updates. Epistemic: stottet / direkte / institusjonell.

**Steps in execution.**

1. **Perspective** — consensus
2. **Epistemics** — stottet / direkte / institusjonell
3. **Social mechanism** — Safety incidents are reported when they are spectacular; the quiet everyday is not counted

#### TE · verden.teknologi

**In one line.** global news streams coded to topic, actor and place · perspective: consensus

**What it does.** the GKG pipeline; coded documents per topic — proxy chain: raw news text -> GKG codes (theme, actor, place) -> share per domain

**How it's built.** Buffer role: GDELT GKG is the buffer: it holds the state between updates. Epistemic: stottet / direkte / institusjonell.

**Steps in execution.**

1. **Perspective** — consensus
2. **Epistemics** — stottet / direkte / institusjonell
3. **Social mechanism** — Technology news is future-oriented: the share measures expectation, not adoption

#### VT · verden.transport

**In one line.** global news streams coded to topic, actor and place · perspective: consensus

**What it does.** the GKG pipeline; coded documents per topic — proxy chain: raw news text -> GKG codes (theme, actor, place) -> share per domain

**How it's built.** Buffer role: GDELT GKG is the buffer: it holds the state between updates. Epistemic: stottet / direkte / institusjonell.

**Steps in execution.**

1. **Perspective** — consensus
2. **Epistemics** — stottet / direkte / institusjonell
3. **Social mechanism** — Transport is measured where it is registered — informal and local transport is invisible

#### UT · verden.utdanning

**In one line.** global news streams coded to topic, actor and place · perspective: consensus

**What it does.** the GKG pipeline; coded documents per topic — proxy chain: raw news text -> GKG codes (theme, actor, place) -> share per domain

**How it's built.** Buffer role: GDELT GKG is the buffer: it holds the state between updates. Epistemic: stottet / direkte / institusjonell.

**Steps in execution.**

1. **Perspective** — consensus
2. **Epistemics** — stottet / direkte / institusjonell
3. **Social mechanism** — Education statistics measure schooling, not learning — and countries that do not report become invisible in the same…

#### GG · verden.klima_gdelt

**In one line.** climate-related coverage coded in global news documents · perspective: consensus

**What it does.** GDELT GKG coding of document, topic, actor and place — proxy chain: news document -> GKG codes -> climate-related observation

**How it's built.** Buffer role: the GDELT corpus holds the coding between messages. Epistemic: stottet / direkte / institusjonell.

**Steps in execution.**

1. **Perspective** — consensus
2. **Epistemics** — stottet / direkte / institusjonell
3. **Social mechanism** — the GDELT project carries the measurement practice and the stream makes it available; this is a social channel, not the…

#### WB · verden.klima_worldbank

**In one line.** climate and development indicators per country and year · perspective: consensus

**What it does.** the World Bank indicators' reported and harmonised datasets — proxy chain: national reporting -> harmonisation -> indicator value -> state message

**How it's built.** Buffer role: harmonised indicator series hold the state between annual updates. Epistemic: stottet / direkte / institusjonell.

**Steps in execution.**

1. **Perspective** — consensus
2. **Epistemics** — stottet / direkte / institusjonell
3. **Social mechanism** — World Bank carries the climate's institutional measurement practice; the stream makes the country and year indicators…

#### MX · verden.miljo_mikrobiom

**In one line.** microbial taxa and functional profiles across sampled environments · perspective: consensus

**What it does.** MGnify microbiome profiles — proxy chain: sample -> sequence processing -> taxonomic and functional profile -> state message

**How it's built.** Buffer role: the MGnify source holds the measurement contract between messages. Epistemic: stottet / direkte / institusjonell.

**Steps in execution.**

1. **Perspective** — consensus
2. **Epistemics** — stottet / direkte / institusjonell
3. **Social mechanism** — MGnify carries the measurement practice and the stream makes it available; this is a social channel, not the truth…

#### GY · verden.miljo_gdelt

**In one line.** environment-related coverage encoded in global news documents · perspective: consensus

**What it does.** GDELT GKG coding of documents, themes, actors and places — proxy chain: news document -> GKG codes -> environmental observation

**How it's built.** Buffer role: the GDELT source holds the measurement contract between messages. Epistemic: stottet / direkte / institusjonell.

**Steps in execution.**

1. **Perspective** — consensus
2. **Epistemics** — stottet / direkte / institusjonell
3. **Social mechanism** — GDELT carries the environment's news measurement practice; the stream makes coded coverage available, but this is not…

#### OX · verden.oekonomi_worldbank

**In one line.** economic indicators by country and year · perspective: consensus

**What it does.** World Bank reported and harmonized indicator datasets — proxy chain: national reporting -> harmonization -> indicator value -> state message

**How it's built.** Buffer role: the World Bank source holds the measurement contract between messages. Epistemic: stottet / direkte / institusjonell.

**Steps in execution.**

1. **Perspective** — consensus
2. **Epistemics** — stottet / direkte / institusjonell
3. **Social mechanism** — World Bank carries the economy's institutional measurement practice; the stream makes the country and year indicators…

#### IX · verden.oekonomi_imf

**In one line.** macroeconomic projections and scenario indicators · perspective: consensus

**What it does.** IMF DataMapper projections and macroeconomic scenarios — proxy chain: institutional projection -> DataMapper series -> forecast reading

**How it's built.** Buffer role: the IMF DataMapper source holds the measurement contract between messages. Epistemic: stottet / direkte / institusjonell.

**Steps in execution.**

1. **Perspective** — consensus
2. **Epistemics** — stottet / direkte / institusjonell
3. **Social mechanism** — IMF DataMapper carries the measurement practice and the stream makes it available; this is a social channel, not the…

#### OY · verden.oekonomi_gdelt

**In one line.** economic coverage encoded in global news documents · perspective: consensus

**What it does.** GDELT GKG coding of documents, themes, actors and places — proxy chain: news document -> GKG codes -> economic observation

**How it's built.** Buffer role: the GDELT source holds the measurement contract between messages. Epistemic: stottet / direkte / institusjonell.

**Steps in execution.**

1. **Perspective** — consensus
2. **Epistemics** — stottet / direkte / institusjonell
3. **Social mechanism** — GDELT carries the economy's news measurement practice; the stream makes coded coverage available, but this is not the…

#### IB · verden.klima_isbre

**In one line.** glacier/glaciology observations when the stream exists · perspective: consensus

**What it does.** waits for a connector — proxy chain: observasjon.isbre -> (waits for stream)

**How it's built.** Buffer role: the observation catalog holds metadata between extractions. Epistemic: stottet / direkte / institusjonell.

**Steps in execution.**

1. **Perspective** — consensus
2. **Epistemics** — stottet / direkte / institusjonell
3. **Social mechanism** — Glaciology's field and remote-sensing community is the expected channel; the glacier observation does not exist on the…

**Questions.**

- **Q-IB1** verden.klima_isbre: the stream does not exist — waits for a connector

### Epistemics

### No group yet

#### SO · h2o.solid _(not switched on)_

**In one line.** thermodynamic phase · perspective: academia

**What it does.** calibrated thermometer + barometer (calibrated against the triple-point cell) — proxy chain: temperature via thermal expansion (thermometer) -> pressure via a membrane (barometer) ->…

**How it's built.** Buffer role: the crystal lattice + latent heat L_f: the ice holds the drink at 0 C until the… Epistemic: stottet / replikert / institusjonell.

**Steps in execution.**

1. **Perspective** — academia
2. **Epistemics** — stottet / replikert / institusjonell
3. **Social mechanism** — peer review, textbook canonisation, career incentives — academia tells what survives the assessment

**Questions.**

- **Q-SO1** h2o.solid: no bus route — the subject does not exist as a domain in the snapshot (measured 2026-09-18)

#### LI · h2o.liquid _(not switched on)_

**In one line.** thermodynamic phase · perspective: academia

**What it does.** calibrated thermometer + barometer — proxy chain: temperature via thermal expansion -> pressure via a membrane -> phase identified via…

**How it's built.** Buffer role: high heat capacity (4.18 kJ/(kg*K)): water holds the temperature during heating… Epistemic: stottet / replikert / institusjonell.

**Steps in execution.**

1. **Perspective** — academia
2. **Epistemics** — stottet / replikert / institusjonell
3. **Social mechanism** — peer review, textbook canonisation, career incentives — academia tells what survives the assessment

**Questions.**

- **Q-LI1** h2o.liquid: no bus route — the subject does not exist as a domain in the snapshot (measured 2026-09-18)

#### GA · h2o.gas _(not switched on)_

**In one line.** thermodynamic phase · perspective: academia

**What it does.** calibrated thermometer + barometer (possibly a hygrometer for vapour) — proxy chain: temperature via thermal expansion -> pressure via a membrane -> vapor content via dew…

**How it's built.** Buffer role: the gas's heat capacity and expansion damp local pressure and temperature… Epistemic: stottet / replikert / institusjonell.

**Steps in execution.**

1. **Perspective** — academia
2. **Epistemics** — stottet / replikert / institusjonell
3. **Social mechanism** — peer review, textbook canonisation, career incentives — academia tells what survives the assessment

**Questions.**

- **Q-GA1** h2o.gas: no bus route — the subject does not exist as a domain in the snapshot (measured 2026-09-18)

#### SC · h2o.supercritical _(not switched on)_

**In one line.** thermodynamic state (not 'phase' — the boundary is gone) · perspective: academia

**What it does.** high-pressure P-T cell — proxy chain: temperature via thermocouple -> pressure via a high-pressure membrane -> no phase…

**How it's built.** Buffer role: no phase boundary to hold — the buffer capacity is continuous, with no latent… Epistemic: stottet / replikert / institusjonell.

**Steps in execution.**

1. **Perspective** — academia
2. **Epistemics** — stottet / replikert / institusjonell
3. **Social mechanism** — peer review, textbook canonisation, career incentives — academia tells what survives the assessment

**Questions.**

- **Q-SC1** h2o.supercritical: no bus route — the subject does not exist as a domain in the snapshot (measured 2026-09-18)

#### TP · h2o.triple_point _(not switched on)_

**In one line.** the temperature scale's reference point · perspective: academia

**What it does.** calibrated triple-point cell — the ITS-90 reference — proxy chain: pressure held constant (611.657 Pa) -> temperature read as the cell wall's thermal…

**How it's built.** Buffer role: the point absorbs energy with no temperature rise as long as three phases… Epistemic: stottet / replikert / institusjonell.

**Steps in execution.**

1. **Perspective** — academia
2. **Epistemics** — stottet / replikert / institusjonell
3. **Social mechanism** — peer review, textbook canonisation, career incentives — academia tells what survives the assessment

**Questions.**

- **Q-TP1** h2o.triple_point: no bus route — the subject does not exist as a domain in the snapshot (measured 2026-09-18)

#### LY · lys.sol _(not switched on)_

**In one line.** electromagnetic radiation · perspective: academia

**What it does.** prism/grating spectrometer — proxy chain: wavelength via grating dispersion -> intensity via detector -> colour as an observer proxy…

**How it's built.** Buffer role: the sun is an enormous energy source with an approximately stable spectral… Epistemic: stottet / replikert / institusjonell.

**Steps in execution.**

1. **Perspective** — academia
2. **Epistemics** — stottet / replikert / institusjonell
3. **Social mechanism** — peer review, textbook canonisation, career incentives — academia tells what survives the assessment

**Questions.**

- **Q-LY1** lys.sol: no bus route — the subject does not exist as a domain in the snapshot (measured 2026-09-18)

#### DR · h2o.droplet _(not switched on)_

**In one line.** drop shape and refractive index · perspective: academia

**What it does.** high-speed camera / refractometer — proxy chain: droplet shape via surface tension -> refractive index via refraction (n ~ 1.33) -> droplet…

**How it's built.** Buffer role: the surface tension holds the droplet spherical — a geometric buffer that damps… Epistemic: stottet / replikert / institusjonell.

**Steps in execution.**

1. **Perspective** — academia
2. **Epistemics** — stottet / replikert / institusjonell
3. **Social mechanism** — peer review, textbook canonisation, career incentives — academia tells what survives the assessment

**Questions.**

- **Q-DR1** h2o.droplet: no bus route — the subject does not exist as a domain in the snapshot (measured 2026-09-18)

#### OP · optikk.dispersjon _(not switched on)_

**In one line.** the refractive index n(lambda) · perspective: academia

**What it does.** spectrometer + prism — proxy chain: deflection angle via Snell's law -> n(lambda) via angular measurement -> colour separation…

**How it's built.** Buffer role: water's electronic structure gives the dispersion stability — n(lambda) is a… Epistemic: stottet / replikert / institusjonell.

**Steps in execution.**

1. **Perspective** — academia
2. **Epistemics** — stottet / replikert / institusjonell
3. **Social mechanism** — peer review, textbook canonisation, career incentives — academia tells what survives the assessment

**Questions.**

- **Q-OP1** optikk.dispersjon: no bus route — the subject does not exist as a domain in the snapshot (measured 2026-09-18)

#### RB · regnbue _(not switched on)_

**In one line.** the bow's angle and colour order · perspective: academia

**What it does.** retina or CCD sensor — proxy chain: colour via wavelength dispersion in the droplet -> arc angle via refraction geometry (~42…

**How it's built.** Buffer role: the droplet swarm is a statistical buffer — the pattern survives individual… Epistemic: stottet / replikert / institusjonell.

**Steps in execution.**

1. **Perspective** — academia
2. **Epistemics** — stottet / replikert / institusjonell
3. **Social mechanism** — peer review, textbook canonisation, career incentives — academia tells what survives the assessment

**Questions.**

- **Q-RB1** regnbue: no bus route — the subject does not exist as a domain in the snapshot (measured 2026-09-18)

#### OB · regnbue.observator _(not switched on)_

**In one line.** the arc as a direction and colour pattern · perspective: academia

**What it does.** retina (S, M and L cones) — proxy chain: photons -> photoreceptors -> S/M/L response -> colour experience -> direction -> arc…

**How it's built.** Buffer role: the eye's adaptation (pupil, bleaching of photopigment) buffers against light… Epistemic: stottet / replikert / institusjonell.

**Steps in execution.**

1. **Perspective** — academia
2. **Epistemics** — stottet / replikert / institusjonell
3. **Social mechanism** — peer review, textbook canonisation, career incentives — academia tells what survives the assessment

**Questions.**

- **Q-OB1** regnbue.observator: no bus route — the subject does not exist as a domain in the snapshot (measured 2026-09-18)

#### L1 · efc.l1 _(not switched on)_

**In one line.** CMB anisotropies and BAO scale · perspective: paradigm

**What it does.** CMB map + galaxy survey — proxy chain: temperature anisotropies -> P(k) -> BAO scale -> H(z) -> polarisation -> optical depth

**How it's built.** Buffer role: releases the photons at z ~ 1100 — the plasma's photon-electron coupling keeps… Epistemic: hypotese / proxy / minoritet. S-axis: regime S~0 · clarity C(S) at S~0: incipient clarity — the threshold of differentiation · EBE claim validity = f(S, L, proxy-chain).

**Steps in execution.**

1. **Perspective** — paradigm
2. **Epistemics** — hypotese / proxy / minoritet
3. **Social mechanism** — our own frame — carried by us, not by the field; the narrative is our own, and it is a strength to know it

**Questions.**

- **Q-L11** efc.l1: no bus route — the subject does not exist as a domain in the snapshot (measured 2026-09-18)

#### L2 · efc.l2 _(not switched on)_

**In one line.** fsigma8, P(k), weak lensing (S8) · perspective: paradigm

**What it does.** galaxy survey + weak lensing (KiDS, DES, Euclid) — proxy chain: galaxy distribution -> P(k) full-shape -> redshift-space distortion -> fsigma8 -> shear ->…

**How it's built.** Buffer role: the structure itself is an inertia buffer: galaxies and clusters hold mass… Epistemic: hypotese / proxy / minoritet. S-axis: regime S>0 · clarity C(S) at S>0: differentiated clarity — the window of reflection, R above R_c~0.37 gives self-modelling · EBE claim validity = f(S, L, proxy-chain).

**Steps in execution.**

1. **Perspective** — paradigm
2. **Epistemics** — hypotese / proxy / minoritet
3. **Social mechanism** — our own frame — carried by us, not by the field; the narrative is our own, and it is a strength to know it

**Questions.**

- **Q-L21** efc.l2: no bus route — the subject does not exist as a domain in the snapshot (measured 2026-09-18)

#### L3 · efc.l3 _(not switched on)_

**In one line.** the far-future state · perspective: paradigm

**What it does.** none direct — predicted limit — proxy chain: the growth law's asymptote -> the S->1 state -> no measurable proxy today

**How it's built.** Buffer role: the saturation IS a buffer: growth is braked against a limit instead of running… Epistemic: hypotese / proxy / minoritet. S-axis: regime S->1 · clarity C(S) at S->1: saturated clarity — the altularity limit, decreasing energy density · EBE claim validity = f(S, L, proxy-chain).

**Steps in execution.**

1. **Perspective** — paradigm
2. **Epistemics** — hypotese / proxy / minoritet
3. **Social mechanism** — our own frame — carried by us, not by the field; the narrative is our own, and it is a strength to know it

**Questions.**

- **Q-L31** efc.l3: no bus route — the subject does not exist as a domain in the snapshot (measured 2026-09-18)

#### BA · obs.bao _(not switched on)_

**In one line.** the BAO scale (r_d ~ 147 Mpc comoving) · perspective: consensus

**What it does.** galaxy surveys (DESI, eBOSS, BOSS) — proxy chain: galaxy correlation function -> BAO peak at ~150 Mpc -> BAO peak -> D_H(z)/r_d, D_M(z)/r_d…

**How it's built.** Buffer role: the drag epoch freezes the sound scale into the plasma — the ruler is frozen in… Epistemic: modellrelativ / proxy / institusjonell. S-axis: RCMP instrument=galaxy surveys (DESI, eBOSS, BOSS); observabel=the BAO scale (r_d ~ 147 Mpc comoving); teori=BAO — the standard ruler; overlap=True; deklarasjon=RCMP: instrument, observable and theory have overlapping domains of validity.

**Steps in execution.**

1. **Perspective** — consensus
2. **Epistemics** — modellrelativ / proxy / institusjonell
3. **Social mechanism** — The BAO peak was canonized via SDSS/BOSS/eBOSS and DESI — large collaborations with NSF/DOE funding; the survey…

#### TT · obs.cmb_tt _(not switched on)_

**In one line.** the temperature/polarisation spectrum · perspective: consensus

**What it does.** Planck — proxy chain: temperature spectrum -> P(k) -> peak distance -> theta_* and r_s(z_*) -> polarisation ->…

**How it's built.** Buffer role: recombination freezes the photons — the signal is held until z ~ 1100, when it… Epistemic: modellrelativ / proxy / institusjonell. S-axis: RCMP instrument=Planck; observabel=the temperature/polarisation spectrum; teori=CMB TT/EE/TE acoustic peaks; overlap=True; deklarasjon=RCMP: instrument, observable and theory have overlapping domains of validity.

**Steps in execution.**

1. **Perspective** — consensus
2. **Epistemics** — modellrelativ / proxy / institusjonell
3. **Social mechanism** — The CMB temperature spectrum is carried by the Planck collaboration's institutional authority — the result was canonised…

#### LC · obs.cmb_lensing _(not switched on)_

**In one line.** the lensing potential · perspective: consensus

**What it does.** Planck, ACT, SPT — proxy chain: CMB anisotropy distribution -> lensing potential -> lensing potential -> P(k) at z ~ 2

**How it's built.** Buffer role: the photons' free journey is the buffer — they accumulate mass imprints along… Epistemic: modellrelativ / proxy / institusjonell. S-axis: RCMP instrument=Planck, ACT, SPT; observabel=the lensing potential; teori=CMB Lensing Reconstruction; overlap=True; deklarasjon=RCMP: instrument, observable and theory have overlapping domains of validity.

**Steps in execution.**

1. **Perspective** — consensus
2. **Epistemics** — modellrelativ / proxy / institusjonell
3. **Social mechanism** — The lensing consensus is carried by Planck and ACT/SPT — two competing instrument groups that confirm each other…

#### BB · obs.bbn _(not switched on)_

**In one line.** the deuterium/hydrogen ratio · perspective: consensus

**What it does.** quasar spectroscopy — proxy chain: D/H in quasar clouds -> baryon density omega_b h^2 -> omega_b h^2 -> cross-check against…

**How it's built.** Buffer role: the nuclear reactions freeze D/H at T ~ 80 keV — a signal that never changes. Epistemic: modellrelativ / proxy / institusjonell. S-axis: RCMP instrument=quasar spectroscopy; observabel=the deuterium/hydrogen ratio; teori=BBN light elements; overlap=True; deklarasjon=RCMP: instrument, observable and theory have overlapping domains of validity.

**Steps in execution.**

1. **Perspective** — consensus
2. **Epistemics** — modellrelativ / proxy / institusjonell
3. **Social mechanism** — BBN is canonised in textbooks and carried by the aesthetic connection with the CMB — re-testing it yields little…

#### F8 · obs.fsigma8 _(not switched on)_

**In one line.** the growth rate fsigma8 · perspective: consensus

**What it does.** BOSS, eBOSS, DESI — proxy chain: redshift-space distortion (RSD) -> f sigma8 -> f sigma8(z) -> the form of the growth law

**How it's built.** Buffer role: the structure itself is the buffer — gravitational response holds mass against… Epistemic: modellrelativ / proxy / institusjonell. S-axis: RCMP instrument=BOSS, eBOSS, DESI; observabel=the growth rate fsigma8; teori=f-sigma-8(z) linear growth; overlap=True; deklarasjon=RCMP: instrument, observable and theory have overlapping domains of validity.

**Steps in execution.**

1. **Perspective** — consensus
2. **Epistemics** — modellrelativ / proxy / institusjonell
3. **Social mechanism** — the fσ8 measurements come from large survey collaborations — the career paths lie in the collaborations, and systematics…

#### S8 · obs.s8 _(not switched on)_

**In one line.** S8 = sigma8 sqrt(omega_m/0.3) · perspective: consensus

**What it does.** KiDS, DES, HSC, Euclid — proxy chain: shear (weak lensing) -> S8 -> low S8 vs CMB expectation -> tension

**How it's built.** Buffer role: the lens images hold the imprint of mass along the line of sight — an… Epistemic: modellrelativ / proxy / institusjonell. S-axis: RCMP instrument=KiDS, DES, HSC, Euclid; observabel=S8 = sigma8 sqrt(omega_m/0.3); teori=S8-tension; overlap=True; deklarasjon=RCMP: instrument, observable and theory have overlapping domains of validity.

**Steps in execution.**

1. **Perspective** — consensus
2. **Epistemics** — modellrelativ / proxy / institusjonell
3. **Social mechanism** — The S8 tension is disputed BETWEEN instrument traditions — two communities with their own careers read the same data…

#### EG · obs.eg _(not switched on)_

**In one line.** the E_G crossing · perspective: consensus

**What it does.** SDSS, KiDS+BOSS — proxy chain: lens (kappa) x RSD (beta) -> E_G -> E_G -> slip between light and mass

**How it's built.** Buffer role: two independent proxies buffer each other — the crossing is more robust than… Epistemic: modellrelativ / proxy / institusjonell. S-axis: RCMP instrument=SDSS, KiDS+BOSS; observabel=the E_G crossing; teori=E_G gravitational-slip statistics; overlap=True; deklarasjon=RCMP: instrument, observable and theory have overlapping domains of validity.

**Steps in execution.**

1. **Perspective** — consensus
2. **Epistemics** — modellrelativ / proxy / institusjonell
3. **Social mechanism** — EG measurements depend on few instruments — the narrative is carried by a small group of specialists with high…

#### IS · obs.isw _(not switched on)_

**In one line.** the ISW signal · perspective: consensus

**What it does.** CMB x galaxy map — proxy chain: CMB map x galaxy distribution -> ISW cross

**How it's built.** Buffer role: the photons integrate the potential's time derivative over the passage — the… Epistemic: modellrelativ / proxy / institusjonell. S-axis: RCMP instrument=CMB x galaxy map; observabel=the ISW signal; teori=ISW cross-correlation; overlap=True; deklarasjon=RCMP: instrument, observable and theory have overlapping domains of validity.

**Steps in execution.**

1. **Perspective** — consensus
2. **Epistemics** — modellrelativ / proxy / institusjonell
3. **Social mechanism** — The ISW signal is weak and was long disputed — the consensus grew with the authority of the CMB tradition, not with new…

#### KS · obs.ksz _(not switched on)_

**In one line.** the kSZ signal · perspective: consensus

**What it does.** ACT, DES x SPT — proxy chain: CMB x galaxies -> kSZ -> kSZ -> peculiar velocity field

**How it's built.** Buffer role: the electrons in clusters Doppler-scatter photons — the plasma is the buffer. Epistemic: modellrelativ / proxy / institusjonell. S-axis: RCMP instrument=ACT, DES x SPT; observabel=the kSZ signal; teori=kSZ peculiar velocities; overlap=True; deklarasjon=RCMP: instrument, observable and theory have overlapping domains of validity.

**Steps in execution.**

1. **Perspective** — consensus
2. **Epistemics** — modellrelativ / proxy / institusjonell
3. **Social mechanism** — kSZ is a young tradition — the consensus is institutional before it is replicated; few groups have the instruments

#### CM · obs.cluster_mass _(not switched on)_

**In one line.** M_500 from several channels · perspective: consensus

**What it does.** Chandra, XMM, HST — proxy chain: X-ray (T_X) -> mass -> weak lensing -> mass -> scaling relations -> mass

**How it's built.** Buffer role: the cluster's potential keeps the gas hot and the light bent — two buffers, one… Epistemic: modellrelativ / proxy / institusjonell. S-axis: RCMP instrument=Chandra, XMM, HST; observabel=M_500 from several channels; teori=cluster-mass scaling; overlap=True; deklarasjon=RCMP: instrument, observable and theory have overlapping domains of validity.

**Steps in execution.**

1. **Perspective** — consensus
2. **Epistemics** — modellrelativ / proxy / institusjonell
3. **Social mechanism** — The halo masses depend on X-ray/weak-lensing calibrations from a few large collaborations — the consensus inherits their…

#### HM · obs.cluster_hmf _(not switched on)_

**In one line.** the halo mass density function · perspective: consensus

**What it does.** DES, SDSS, eROSITA — proxy chain: number of clusters per mass -> N(M,z) -> N(M,z) -> sigma8 and growth

**How it's built.** Buffer role: the collapse buffers mass in halos — the count is the buffers' distribution. Epistemic: modellrelativ / proxy / institusjonell. S-axis: RCMP instrument=DES, SDSS, eROSITA; observabel=the halo mass density function; teori=cluster-halo mass density N(M,z); overlap=True; deklarasjon=RCMP: instrument, observable and theory have overlapping domains of validity.

**Steps in execution.**

1. **Perspective** — consensus
2. **Epistemics** — modellrelativ / proxy / institusjonell
3. **Social mechanism** — The halo function is anchored in numerical simulations with their own code traditions — the consensus is carried by the…

#### RA · obs.rar _(not switched on)_

**In one line.** a_obs vs a_bar · perspective: consensus

**What it does.** SPARC — proxy chain: rotation curves -> a_obs -> baryonic distribution -> a_bar -> deviation -> a_obs/a_bar

**How it's built.** Buffer role: the galaxy's potential holds the rotation — the dynamics are the buffer. Epistemic: modellrelativ / proxy / institusjonell. S-axis: RCMP instrument=SPARC; observabel=a_obs vs a_bar; teori=Radial Acceleration Relation; overlap=True; deklarasjon=RCMP: instrument, observable and theory have overlapping domains of validity.

**Steps in execution.**

1. **Perspective** — consensus
2. **Epistemics** — modellrelativ / proxy / institusjonell
3. **Social mechanism** — RAR (radial acceleration relation) is contested in the MOND debate — two communities with incompatible narratives read…

#### BU · obs.bullet _(not switched on)_

**In one line.** the mass-gas offset · perspective: consensus

**What it does.** HST + Chandra — proxy chain: lensing (mass) vs X-ray (gas) -> offset delta-kappa

**How it's built.** Buffer role: the collision separates the components — the event is the buffer. Epistemic: modellrelativ / proxy / institusjonell. S-axis: RCMP instrument=HST + Chandra; observabel=the mass-gas offset; teori=the Bullet cluster delta-kappa; overlap=True; deklarasjon=RCMP: instrument, observable and theory have overlapping domains of validity.

**Steps in execution.**

1. **Perspective** — consensus
2. **Epistemics** — modellrelativ / proxy / institusjonell
3. **Social mechanism** — The Bullet Cluster is read in the MOND debate — two frames with their own career paths; the image is the same, the story…

#### SL · obs.satellites _(not switched on)_

**In one line.** the subhalo population · perspective: consensus

**What it does.** HST, simulations — proxy chain: counted dwarf satellites vs predicted -> shortfall -> dense cores vs cusp -> profile

**How it's built.** Buffer role: subhalos are held by the host's potential — the buffer is tidal stripping. Epistemic: modellrelativ / proxy / institusjonell. S-axis: RCMP instrument=HST, simulations; observabel=the subhalo population; teori=missing satellites / TBTF / core-cusp; overlap=True; deklarasjon=RCMP: instrument, observable and theory have overlapping domains of validity.

**Steps in execution.**

1. **Perspective** — consensus
2. **Epistemics** — modellrelativ / proxy / institusjonell
3. **Social mechanism** — The satellite problem is carried by the simulation tradition vs the observers — a known point of tension where the…

#### JW · obs.jwst_ems _(not switched on)_

**In one line.** the galaxy mass function at z>10 · perspective: consensus

**What it does.** JWST — proxy chain: JWST photometry -> mass at z>10 -> mass function -> collapse timescale

**How it's built.** Buffer role: early halos are the first buffers — the first to hold mass. Epistemic: modellrelativ / proxy / institusjonell. S-axis: RCMP instrument=JWST; observabel=the galaxy mass function at z>10; teori=early massive structure (JWST z>10); overlap=True; deklarasjon=RCMP: instrument, observable and theory have overlapping domains of validity.

**Steps in execution.**

1. **Perspective** — consensus
2. **Epistemics** — modellrelativ / proxy / institusjonell
3. **Social mechanism** — The JWST findings are new and are negotiated OPENLY — every «unexpected» finding yields publicity and thereby an…

#### GW · obs.gw_ct _(not switched on)_

**In one line.** c_T from GW170817 · perspective: consensus

**What it does.** LIGO/Virgo — proxy chain: GW and gamma arrival -> c_T/c within 1e-15

**How it's built.** Buffer role: the 1.7-second delay over 40 Mpc is the measurement's buffer — the journey… Epistemic: modellrelativ / proxy / institusjonell. S-axis: RCMP instrument=LIGO/Virgo; observabel=c_T from GW170817; teori=Gravitational-wave speed c_T; overlap=True; deklarasjon=RCMP: instrument, observable and theory have overlapping domains of validity.

**Steps in execution.**

1. **Perspective** — consensus
2. **Epistemics** — modellrelativ / proxy / institusjonell
3. **Social mechanism** — Gravitational waves are a young, rapidly institutionalized tradition — the LIGO/Virgo/KAGRA collaborations hold a…

#### PA · obs.pta_gwb _(not switched on)_

**In one line.** the GW background at nHz · perspective: consensus

**What it does.** NANOGrav, EPTA — proxy chain: pulsar timing residuals -> Hellings-Downs correlation -> GW background

**How it's built.** Buffer role: the pulsars ARE the buffer — their rotation holds the phase over years. Epistemic: modellrelativ / proxy / institusjonell. S-axis: RCMP instrument=NANOGrav, EPTA; observabel=the GW background at nHz; teori=PTA gravitational-wave background; overlap=True; deklarasjon=RCMP: instrument, observable and theory have overlapping domains of validity.

**Steps in execution.**

1. **Perspective** — consensus
2. **Epistemics** — modellrelativ / proxy / institusjonell
3. **Social mechanism** — The PTA consensus is built by a few large collaborations with decade-long data sets — the data are private until…

#### H0 · obs.h0_tension _(not switched on)_

**In one line.** H0 from two independent channels · perspective: consensus

**What it does.** Planck vs SH0ES — proxy chain: CMB (LCDM extrapolation) -> H0 ~ 67 -> cepheid/SN ladder -> H0 ~ 73 -> gap -> tension

**How it's built.** Buffer role: two independent measurement chains buffer each other — which is exactly why… Epistemic: modellrelativ / proxy / institusjonell. S-axis: RCMP instrument=Planck vs SH0ES; observabel=H0 from two independent channels; teori=Hubble-tension H0; overlap=True; deklarasjon=RCMP: instrument, observable and theory have overlapping domains of validity.

**Steps in execution.**

1. **Perspective** — consensus
2. **Epistemics** — modellrelativ / proxy / institusjonell
3. **Social mechanism** — The Hubble tension is CURRENTLY contested — distance scale vs CMB, two career paths; every new measurement shifts the…

#### W0 · obs.w0wa _(not switched on)_

**In one line.** the w(z) parametrisation · perspective: consensus

**What it does.** DES, BAO+CMB — proxy chain: SN + BAO + CMB -> w0, wa -> w0, wa -> deviation from -1

**How it's built.** Buffer role: the stretch of SN light over distance is the buffer — the expansion history is… Epistemic: modellrelativ / proxy / institusjonell. S-axis: RCMP instrument=DES, BAO+CMB; observabel=the w(z) parametrisation; teori=w0-wa dynamical dark energy; overlap=True; deklarasjon=RCMP: instrument, observable and theory have overlapping domains of validity.

**Steps in execution.**

1. **Perspective** — consensus
2. **Epistemics** — modellrelativ / proxy / institusjonell
3. **Social mechanism** — the dark-energy parametrisation is a consensus CONVENTION more than a measurement — the narrative is carried by the…

#### CC · obs.cc _(not switched on)_

**In one line.** the size of the vacuum energy · perspective: consensus

**What it does.** all channels — proxy chain: all observations -> omega_Lambda ~ 0.7 -> naive theoretical QFT estimate -> ~120 orders of…

**How it's built.** Buffer role: the background's acceleration is the buffer — it holds the expansion up. Epistemic: modellrelativ / proxy / institusjonell. S-axis: RCMP instrument=all channels; observabel=the size of the vacuum energy; teori=cosmological constant / vacuum energy; overlap=True; deklarasjon=RCMP: instrument, observable and theory have overlapping domains of validity.

**Steps in execution.**

1. **Perspective** — consensus
2. **Epistemics** — modellrelativ / proxy / institusjonell
3. **Social mechanism** — the cluster count's consensus is carried by the mass-calibration chain — a long chain of assumptions each of which is…

#### HF · homo.fluxus _(not switched on)_

**In one line.** R — the internal reflection coefficient · perspective: paradigm

**What it does.** the hf1–hf5 observation requirements (falsifiable claims in the framework) — proxy chain: coherence proxies → R (inference) -> R → flow object/flow subject (threshold)

**How it's built.** Buffer role: the body's buffers (thermal, chemical, neural) hold the flow pattern stable… Epistemic: hypotese / proxy / minoritet. S-axis: regime S~0.5 · clarity C(S) at S~0.5: differentiated clarity in the late universe's biological intermediate layer — the observer sees a narrow…

**Steps in execution.**

1. **Perspective** — paradigm
2. **Epistemics** — hypotese / proxy / minoritet
3. **Social mechanism** — our own frame — carried by us, not by the field; the narrative is our own, and it is a strength to know it

#### HO · homo.homeostase_buffer _(not switched on)_

**In one line.** deviation from setpoint (ΔT, ΔpH, Δglucose) · perspective: academia

**What it does.** physiological sensors; here the node is a description, not a sensor — proxy chain: sensor → deviation -> deviation → compensatory response

**How it's built.** Buffer role: the buffer itself: capacity that damps change — the broad logic in pure form. Epistemic: stottet / replikert / institusjonell. S-axis: regime S~0.5 · clarity C(S) at S~0.5: differentiated clarity in the late universe's biological intermediate layer — the observer sees a narrow…

**Steps in execution.**

1. **Perspective** — academia
2. **Epistemics** — stottet / replikert / institusjonell
3. **Social mechanism** — Levin 2019 (biology) — peer-reviewed and canonised in textbooks. THE ANALOGY to EFC is our own and is not carried by the…

#### FE · homo.feber_regime _(not switched on)_

**In one line.** body temperature against setpoint · perspective: academia

**What it does.** thermometer + pyrogen markers; here: description — proxy chain: pyrogens → setpoint shift -> temperature → distance to the new setpoint

**How it's built.** Buffer role: the buffer switches TARGET, not capacity — that is the regime shift itself. Epistemic: stottet / replikert / institusjonell. S-axis: regime S~0.5 · clarity C(S) at S~0.5: differentiated clarity in the late universe's biological intermediate layer — the observer sees a narrow…

**Steps in execution.**

1. **Perspective** — academia
2. **Epistemics** — stottet / replikert / institusjonell
3. **Social mechanism** — physiology (standard) — peer-reviewed and canonised in textbooks. The ANALOGY to EFC is our own and is not carried by…

#### AP · homo.aksjonspotensial _(not switched on)_

**In one line.** the membrane potential V(t) against the threshold · perspective: academia

**What it does.** physiological measurement; here description — proxy chain: ion currents → V(t) -> V(t) vs V_th → spike

**How it's built.** Buffer role: the membrane is the buffer: the gradient charges and is held until release. Epistemic: stottet / replikert / institusjonell. S-axis: regime S~0.5 · clarity C(S) at S~0.5: differentiated clarity in the late universe's biological intermediate layer — the observer sees a narrow…

**Steps in execution.**

1. **Perspective** — academia
2. **Epistemics** — stottet / replikert / institusjonell
3. **Social mechanism** — neurophysiology (standard) — peer-reviewed and textbook-canonized. The ANALOGY to EFC is our own and is not carried by…

#### HJ · homo.hjerte_syklus _(not switched on)_

**In one line.** stroke volume, frequency, minute volume · perspective: academia

**What it does.** physiological measurement; here description — proxy chain: ECG → electrical cycle -> echo → mechanical cycle

**How it's built.** Buffer role: the ventricles are the buffers: they fill and empty rhythmically — never to… Epistemic: stottet / replikert / institusjonell. S-axis: regime S~0.5 · clarity C(S) at S~0.5: differentiated clarity in the late universe's biological intermediate layer — the observer sees a narrow…

**Steps in execution.**

1. **Perspective** — academia
2. **Epistemics** — stottet / replikert / institusjonell
3. **Social mechanism** — cardiology (standard) — peer-reviewed and textbook-canonized. The ANALOGY to EFC is our own and is not carried by the…

#### GN · homo.genregulering _(not switched on)_

**In one line.** expression level per gene (mRNA/protein) · perspective: academia

**What it does.** sequencing; here: description of the regulatory logic — proxy chain: TF binding → expression -> epigenetic mark → threshold shift

**How it's built.** Buffer role: the genome keeps the regulatory programs stored — a capacity that damps random… Epistemic: stottet / replikert / institusjonell. S-axis: regime S~0.5 · clarity C(S) at S~0.5: differentiated clarity in the late universe's biological intermediate layer — the observer sees a narrow…

**Steps in execution.**

1. **Perspective** — academia
2. **Epistemics** — stottet / replikert / institusjonell
3. **Social mechanism** — molecular biology (standard) — peer-reviewed and textbook-canonized. The atlas uses it as established, not as its own…

#### CY · homo.cellesyklus _(not switched on)_

**In one line.** phase per cell (G1/S/G2/M) · perspective: academia

**What it does.** laboratory measurement; here description — proxy chain: DNA content → phase -> CDK/cyclin → checkpoint status

**How it's built.** Buffer role: the checkpoints are the buffers: they hold the cycle until the conditions are… Epistemic: stottet / replikert / institusjonell. S-axis: regime S~0.5 · clarity C(S) at S~0.5: differentiated clarity in the late universe's biological intermediate layer — the observer sees a narrow…

**Steps in execution.**

1. **Perspective** — academia
2. **Epistemics** — stottet / replikert / institusjonell
3. **Social mechanism** — cell biology (standard) — peer-reviewed and canonised in textbooks. THE ANALOGY to EFC is our own and is not carried by…

#### ME · homo.metabolisme _(not switched on)_

**In one line.** ATP/ADP ratio, O₂ consumption, substrate flux · perspective: academia

**What it does.** laboratory measurement; here description — proxy chain: O₂ consumption → flux -> ATP/ADP → regulatory status

**How it's built.** Buffer role: The ATP pool and glycogen are the buffers: short- and long-term stores that damp… Epistemic: stottet / replikert / institusjonell. S-axis: regime S~0.5 · clarity C(S) at S~0.5: differentiated clarity in the late universe's biological intermediate layer — the observer sees a narrow…

**Steps in execution.**

1. **Perspective** — academia
2. **Epistemics** — stottet / replikert / institusjonell
3. **Social mechanism** — biochemistry (standard) — peer-reviewed and canonized in textbooks. The ANALOGY to EFC is our own and is not carried by…

#### SF · efc.solar_flare_engine _(not switched on)_

**In one line.** charging time, released energy, GOES class · perspective: paradigm

**What it does.** SolarFlareEngine (efc_inference/engine/solar_flare.py) — proxy chain: B -> magnetic energy (E = B^2/(2 mu_0) * V) -> energy -> GOES class (calibration proxy…

**How it's built.** Buffer role: the magnetic field is the buffer: the energy is charged and held until the… Epistemic: hypotese / proxy / minoritet.

**Steps in execution.**

1. **Perspective** — paradigm
2. **Epistemics** — hypotese / proxy / minoritet
3. **Social mechanism** — our own frame — carried by us, not by the field; the narrative is our own, and it is a strength to know it

#### JS · efc.jordskjelv_engine _(not switched on)_

**In one line.** recurrence time, seismic moment, moment magnitude · perspective: paradigm

**What it does.** JordskjelvEngine (efc_inference/engine/jordskjelv.py) — proxy chain: charge rate -> recurrence time -> stress drop -> slip -> M0 -> Mw (Kanamori)

**How it's built.** Buffer role: the fault is the buffer: the stress charges and is held until the threshold is… Epistemic: hypotese / proxy / minoritet.

**Steps in execution.**

1. **Perspective** — paradigm
2. **Epistemics** — hypotese / proxy / minoritet
3. **Social mechanism** — our own frame — carried by us, not by the field; the narrative is our own, and it is a strength to know it

#### IM · homo.immunologi _(not switched on)_

**In one line.** activation status, antibody titre, memory population · perspective: academia

**What it does.** laboratory measurement; here description — proxy chain: danger signals + antigen concentration -> activation -> titre -> memory

**How it's built.** Buffer role: memory is the buffer: it lowers the threshold and makes the next response faster. Epistemic: stottet / replikert / institusjonell. S-axis: regime S~0.5 · clarity C(S) at S~0.5: differentiated clarity in the late universe's biological intermediate layer — the observer sees a narrow…

**Steps in execution.**

1. **Perspective** — academia
2. **Epistemics** — stottet / replikert / institusjonell
3. **Social mechanism** — immunology (standard: Janeway/Matzinger) — peer-reviewed and textbook-canonized. The atlas uses it as established, not…

#### SV · homo.sovn_vaaken _(not switched on)_

**In one line.** sleep stage (awake/NREM/REM), homeostatic pressure · perspective: academia

**What it does.** clinical measurement; here: description — proxy chain: EEG synchrony -> stage -> wake duration -> homeostatic pressure

**How it's built.** Buffer role: the need for sleep is the buffer: it accumulates while awake and is drained in… Epistemic: stottet / replikert / institusjonell. S-axis: regime S~0.5 · clarity C(S) at S~0.5: differentiated clarity in the late universe's biological intermediate layer — the observer sees a narrow…

**Steps in execution.**

1. **Perspective** — academia
2. **Epistemics** — stottet / replikert / institusjonell
3. **Social mechanism** — sleep physiology (standard: Borbely, Steriade) — peer-reviewed and textbook-canonized. The ANALOGY to EFC is our own and…

#### OE · homo.okologi _(not switched on)_

**In one line.** state variables (nutrients, coverage, species composition) · perspective: academia

**What it does.** ecological monitoring; here description — proxy chain: nutrients -> state -> coverage -> regime

**How it's built.** Buffer role: the ecosystem's buffer capacity (resilience) damps disturbances — until the… Epistemic: stottet / replikert / institusjonell. S-axis: regime S~0.5 · clarity C(S) at S~0.5: differentiated clarity in the late universe's biological intermediate layer — the observer sees a narrow…

**Steps in execution.**

1. **Perspective** — academia
2. **Epistemics** — stottet / replikert / institusjonell
3. **Social mechanism** — regime-shift ecology (Scheffer) — peer-reviewed and canonised in textbooks. The atlas uses it as established, not as its…

#### EV · homo.evolusjon _(not switched on)_

**In one line.** rate of change in phenotype/species (morphological and molecular… · perspective: academia

**What it does.** palaeontological/genomic measurement; here: description — proxy chain: fossil record -> morphological rate -> molecular distance -> time since split

**How it's built.** Buffer role: stasis is holding: selection and developmental constraints hold the phenotype… Epistemic: stottet / replikert / institusjonell. S-axis: regime S~0.5 · clarity C(S) at S~0.5: differentiated clarity in the late universe's biological intermediate layer — the observer sees a narrow…

**Steps in execution.**

1. **Perspective** — academia
2. **Epistemics** — stottet / replikert / institusjonell
3. **Social mechanism** — evolutionary biology (standard) — peer-reviewed and canonised in textbooks. The ANALOGY to EFC is our own and is not…

#### VV · verden.vaer _(not switched on)_

**In one line.** temperature, wind, pressure, cloud cover and humidity · perspective: consensus

**What it does.** one place in one validity window; both expected and outcome in the same message — proxy chain: forecast -> expected -> METAR measurement -> outcome -> deviation = outcome minus…

**How it's built.** Buffer role: the atmosphere as buffer between prediction and reality. Epistemic: stottet / direkte / institusjonell.

**Steps in execution.**

1. **Perspective** — consensus
2. **Epistemics** — stottet / direkte / institusjonell
3. **Social mechanism** — MET Norway forecast against METAR measurement — two independent institutional channels

#### KA · kosmos.asteroider _(not switched on)_

**In one line.** impact probability and Palermo cumulative per object · perspective: consensus

**What it does.** The Sentry list itself; the reconciliation reads the whole list as the answer key — proxy chain: track -> hit probability (prediction) -> list -> revised/excluded (settlement)

**How it's built.** Buffer role: The Sentry list is the buffer that keeps the risk picture stable between… Epistemic: stottet / proxy / institusjonell.

**Steps in execution.**

1. **Perspective** — consensus
2. **Epistemics** — stottet / proxy / institusjonell
3. **Social mechanism** — JPL Sentry as consensus channel

#### EE · efc.efc_background_engine _(not switched on)_

**In one line.** H(z) and the background fields phi, phi_dot, rho_m, lambda_dot · perspective: paradigm

**What it does.** EFCBackgroundSolver (efc_inference/engine/efc_background.py) — proxy chain: parameters (alpha, k0, omega_crit, gamma0, V0) -> ODE system -> ODE system -> state (a, E…

**How it's built.** Buffer role: lambda is the buffer: the response field that keeps the flow accounting when phi… Epistemic: hypotese / ingen / minoritet.

**Steps in execution.**

1. **Perspective** — paradigm
2. **Epistemics** — hypotese / ingen / minoritet
3. **Social mechanism** — our own frame — carried by us, not by the field

#### LS · efc.lag_s _(not switched on)_

**In one line.** the structure that the energy-flow field holds up · perspective: paradigm

**What it does.** none directly — the structure is derived from the field, not measured as structure — proxy chain: energy-flow field -> density gradient -> potential -> structure -> structure -> rotation…

**How it's built.** Buffer role: the halo itself is the buffer: it holds the form while the field varies beneath. Epistemic: hypotese / proxy / minoritet. S-axis: regime S>0 · sector S · clarity C(S) at S>0: structural clarity — the form reads the energy flow at equilibrium · EBE claim validity = f(S, L, proxy-chain).

**Steps in execution.**

1. **Perspective** — paradigm
2. **Epistemics** — hypotese / proxy / minoritet
3. **Social mechanism** — our frame — replaces dark matter, which is a MINORITY position against LCDM

#### LD · efc.lag_d _(not switched on)_

**In one line.** the energy-flow field J_mu and its divergence · perspective: paradigm

**What it does.** the action (master-spec, eq. 1) — proxy chain: action -> field equation -> J_mu -> expansion history -> J_mu -> Sigma (source term)

**How it's built.** Buffer role: F(phi) and K(rho) damp deviations from LCDM depending on the field value. Epistemic: hypotese / proxy / minoritet. S-axis: regime S>0 · sector D · clarity C(S) at S>0: dynamic clarity — time, space and velocity emerge from energy flow under S · EBE claim validity = f(S, L, proxy-chain).

**Steps in execution.**

1. **Perspective** — paradigm
2. **Epistemics** — hypotese / proxy / minoritet
3. **Social mechanism** — our frame — replaces dark energy, which is consensus in LCDM

#### C0 · efc.lag_c0 _(not switched on)_

**In one line.** the entropy field S and its boundaries · perspective: paradigm

**What it does.** none directly — S is a field, not a measurement — proxy chain: S -> Omega-hat (differentiation) and kappa-hat (integration) -> Omega x kappa -> C…

**How it's built.** Buffer role: entropy itself: it absorbs energy without the structure changing, until the… Epistemic: hypotese / proxy / minoritet. S-axis: regime S->1 · sector C · clarity C(S) at S->1: saturated clarity locally — propofol-EEG (Omega, kappa) measures clarity variation at small scale · EBE claim validity = f(S, L, proxy-chain).

**Steps in execution.**

1. **Perspective** — paradigm
2. **Epistemics** — hypotese / proxy / minoritet
3. **Social mechanism** — our frame — the cosmology-to-cognition bridge is not established

## Flows (representative packets)

Payload shapes are what the design implies, not measured traffic.

### Ocean state

| # | From → To | Packet | Representative payload |
|---|---|---|---|
| 1 | HA → KL | temperature proxy | `{"emne":"verden.klima.tilstand.noaa-tides"}` |

### Biosphere counts

| # | From → To | Packet | Representative payload |
|---|---|---|---|
| 1 | BI → EF | species counts | `{"emne":"verden.miljo.tilstand.gbif-planter"}` |

### Volcano state

| # | From → To | Packet | Representative payload |
|---|---|---|---|
| 1 | VU → TR | activity level | `{"emne":"kosmos.jord.tilstand.usgs-vulkan"}` |

## Questions — index

Reference by ID. ✓ resolved (with date) · otherwise open.

- **Q-L01** (L0) efc.l0: no bus route — the subject does not exist as a domain in the snapshot (measured 2026-09-18)
- **Q-GH1** (GH) efc.grid_higgs: no bus route — the subject does not exist as a domain in the snapshot (measured 2026-09-18)
- **Q-GQ1** (GQ) efc.gr_qft_bro: no bus route — the subject does not exist as a domain in the snapshot (measured 2026-09-18)
- **Q-DS1** (DS) efc.double_slit: no bus route — the subject does not exist as a domain in the snapshot (measured 2026-09-18)
- **Q-SH1** (SH) efc.sort_hull: no bus route — the subject does not exist as a domain in the snapshot (measured 2026-09-18)
- **Q-DB1** (DB) kosmos.kosmologi_desi_bao: the stream does not exist — waits for a connector
- **Q-PS1** (PS) kjemi.periodesystemet: no bus route — the subject does not exist as a domain in the snapshot (measured 2026-09-18)
- **Q-IB1** (IB) verden.klima_isbre: the stream does not exist — waits for a connector
- **Q-SO1** (SO) h2o.solid: no bus route — the subject does not exist as a domain in the snapshot (measured 2026-09-18)
- **Q-LI1** (LI) h2o.liquid: no bus route — the subject does not exist as a domain in the snapshot (measured 2026-09-18)
- **Q-GA1** (GA) h2o.gas: no bus route — the subject does not exist as a domain in the snapshot (measured 2026-09-18)
- **Q-SC1** (SC) h2o.supercritical: no bus route — the subject does not exist as a domain in the snapshot (measured 2026-09-18)
- **Q-TP1** (TP) h2o.triple_point: no bus route — the subject does not exist as a domain in the snapshot (measured 2026-09-18)
- **Q-LY1** (LY) lys.sol: no bus route — the subject does not exist as a domain in the snapshot (measured 2026-09-18)
- **Q-DR1** (DR) h2o.droplet: no bus route — the subject does not exist as a domain in the snapshot (measured 2026-09-18)
- **Q-OP1** (OP) optikk.dispersjon: no bus route — the subject does not exist as a domain in the snapshot (measured 2026-09-18)
- **Q-RB1** (RB) regnbue: no bus route — the subject does not exist as a domain in the snapshot (measured 2026-09-18)
- **Q-OB1** (OB) regnbue.observator: no bus route — the subject does not exist as a domain in the snapshot (measured 2026-09-18)
- **Q-L11** (L1) efc.l1: no bus route — the subject does not exist as a domain in the snapshot (measured 2026-09-18)
- **Q-L21** (L2) efc.l2: no bus route — the subject does not exist as a domain in the snapshot (measured 2026-09-18)
- **Q-L31** (L3) efc.l3: no bus route — the subject does not exist as a domain in the snapshot (measured 2026-09-18)

## Known holes

**The bus is read by code, not consumed in drift.** `scripts/atlas_volum.py --maal` reads the JetStream streams' `state.subjects` through the house's own `verden_domener` (MCP) and writes `schema/nats_domener.snapshot.json` with its own provenance: 39 domains, measured 2026-09-17T18:41:27Z by faber (scripts/atlas_volum.py). No schedule runs that measurement: 0 of 16 workflow files in `.github/workflows` reference it, so the snapshot ages by itself. Until a door holding the bus key measures again, the only alarm is `tests/test_atlas_dekning.py::test_snapshottet_har_ikke_gaatt_ut_paa_dato` at 90 days — a stale measurement that still answers, which is the failure mode the atlas exists to name.

**Bus routes in the node bank.** 21 of the published nodes name a bus route; the other 95 say nothing. A named route is a connection the bank has taken a position on, not traffic the atlas has seen.

## What the platform gives vs what we own

**Platform gives:** NATS bus (read by measurement code, scheduled by nothing — see Known holes), engines, review fan-out, the EFC bank.

**We own:** The atlas itself — every node, every epistemic declaration, every threshold.

## Planned filesystem

```
schema/regime_nodes.jsonld\n  efc_inference/engine/*.py\n  efc_inference/bridge/*.py
```

## How this file is maintained

Generated from `schema/regime_nodes.jsonld` by `node docs/efc-atlas/atlas/build.mjs`, which also builds the interactive atlas (`atlas.html`). Edit the data file, rebuild, republish — never edit this file by hand.
