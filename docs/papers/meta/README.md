# Meta-Principle Papers

Cross-layer principles (apply to cosmos + cognitive + medicine). Layer
routes to Neo4j `knowledge_domain='meta'` and Qdrant `efc` collection
(meta lives in cosmos collection per LAYER_PATH_MAP['meta']).

## Subdirectories

| Principle | Description |
|---|---|
| HomoFluxus | Human as energy-flow system |
| Proxy | Cross-layer proxy mechanisms (pharma, mirror, entropy) |
| Autopoiesis | Self-creating systems (Maturana-Varela) |

Note: existing `meta/meta-architecture/`, `meta/meta-process/` etc in
the AGI repo are ALSO meta-layer per A14 (tag in place). This directory
is for new greenfield papers + publications.

Personal meta notes: `/Users/morpheus/Documents/symbiose/meta/`
